// Runs once before each test file after the test framework is set up.
// Adds @testing-library/jest-dom custom matchers like toBeInTheDocument(),
// toHaveTextContent(), toBeDisabled(), etc.
import "@testing-library/jest-dom";
