@AGENTS.md

# Project brief — ga40prj

A web application for managing **People**, **Documents**, and **Properties**, with PostGIS-backed spatial data and bilingual English/Romanian UI. Built one vertical slice at a time. Deployed on Vercel + Supabase; local Docker Postgres remains the primary dev environment.

## How Claude works with Adrian (generic rules)

These apply regardless of which slice is in progress:

- **One vertical slice at a time** — confirm the current slice before writing any code.
- **Wait for approval before writing code** at the start of every session.
- **Provide complete, ready-to-copy code** — components, hooks, schema migrations, API routes. No stubs or placeholders unless explicitly asked.
- **Include TypeScript types, error handling, loading states, and accessibility** on every component.
- **Bilingual by default** — use the established next-intl patterns already in the repo; never hard-code UI strings. The one intentional exception is runtime-editable content (see "Two-track i18n" in Gotchas).
- **Never commit or push without explicit confirmation.** Same for any irreversible action.
- **Conventional commits** — `feat:`, `fix:`, `chore:`, `ci:`, `docs(scope):`, `test:`.
- **Minimise human effort — always.** Processing and storage are cheap; human time and attention are not. Always prefer the most automated solution, even if it does more work. Never ask Adrian to run manual export queries, copy-paste SQL, or track deltas by hand. Build scripts that do the full job. When in doubt, do a full reset rather than a targeted delta — correctness and simplicity matter more than efficiency.
- **Full reset over delta.** For Supabase/cloud sync, always do a complete reset (drop everything, recreate from scratch, re-seed all data) rather than trying to calculate and apply only what changed. A full reset is reliable; delta logic is fragile.
- **Never dismiss errors as "pre-existing", "unrelated to this slice", or "not impacting the current work".** When Adrian reports errors from `npm run lint`, `npx jest`, or `git push` / CI, ALL errors must be fixed before moving on — regardless of which slice introduced them. An unfixed error anywhere in the codebase is a real problem: it masks genuine bugs, breaks CI, and creates compounding debt. There is no such thing as a safe-to-ignore error. Fix every error that appears, then confirm the build is clean.
- **Verification order: `npm run e2e` → `npm run lint` → `npx jest`.** Since Slice #19.28.e2e landed the Playwright suite, always ask Adrian to run `npm run e2e` FIRST, before the usual lint + Jest pair — not just when a change touches Property/Person/Document/versioning, always. Unlike lint and Jest, it needs `npm run dev` already running in a separate terminal — say so every time, since it's easy to forget and the failure mode (every test times out waiting for a page load) looks nothing like "the dev server isn't running." See `e2e/README.md` for what the suite covers and how to read a failure.

## Who you're working with

Adrian is the sole user of this repo. He's a business analyst, not a full-time developer — comfortable reading code, running commands, and reasoning about architecture, but he leans on Claude as a full-stack development partner. He works on Windows (PowerShell), keeps reference docs in `C:\dev.docs\ga40prj` (read-only to Claude), and prefers small, deliberate changes over big rewrites.

**Language:** Adrian prefers all conversations with Claude to be in English only.

## Domain model

Three core objects with multiple many-to-many relationships, including self-referential ones:

- **Person** — individuals or organizations connected to the project
- **Document** — documents, contracts, certificates, etc.
- **Property** — parcels with spatial geometry (points, polygons) stored in PostGIS

Relationships: People ↔ Documents, People ↔ Properties, Documents ↔ Properties, plus self-references (e.g. a Person related to another Person, a Property containing another Property). Field names and type vocabularies live in `messages/en-GB.json` and `messages/ro-RO.json`, served via next-intl.

## Tech stack (locked in)

- **Frontend** — Next.js 16.2.4 (App Router), React 19.2.4, Tailwind CSS v4
- **Data fetching** — TanStack Query 5
- **Forms + validation** — React Hook Form 7 + Zod v4 (`import { z } from "zod/v4"`) via `@hookform/resolvers`
- **Maps** — `@vis.gl/react-google-maps ^1.8.3` (Google Maps JS API React wrapper). Leaflet 1.9.4 + react-leaflet 5 are still installed but no longer used — do not reach for them. The `APIProvider` wraps the whole app in `src/components/providers/maps-provider.tsx`, seeded with `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`.
- **i18n** — next-intl; two locales: `en-GB` and `ro-RO`; cookie-based (no URL segment); messages in `messages/*.json`. **Exception:** `help_content` and `help_hint` store their bilingual text as inline DB columns (`background_en`/`background_ro`, `how_to_en`/`how_to_ro`, `hint_en`/`hint_ro`) instead of in the messages files — see "Two-track i18n" gotcha below.
- **Database** — PostgreSQL 16 + PostGIS 3.4 (Docker image `postgis/postgis:16-3.4`), pgAdmin 4
- **Testing** — Jest 30 with `next/jest` (SWC transformer), jsdom, `@testing-library/react` + `jest-dom`
- **CI** — GitHub Actions: `npm ci` → lint → test → build
- **Cloud target** — Vercel (frontend) + Supabase (Postgres, auth, storage). Live at `https://ga40prj.vercel.app`. Every push to `main` auto-deploys.

## Development methodology

**One vertical slice at a time**, in this order: DB schema/migration → API routes → UI components → tests. Each slice ends with a clean commit history and a green CI run before the next slice begins.

> **Per-slice detail lives in git history, not in this file.** This index lists every slice as a one-liner. The granular per-slice change-logs that used to live below were trimmed to keep CLAUDE.md navigable — recover any of them with `git log -p -- CLAUDE.md` or by reading the relevant commit. Cross-cutting, reusable knowledge is preserved in **Gotchas we've learned** (below) and, for the versioning feature, in **Versioning pattern & pitfalls** (below).

**Slice progress (index)**

- Slice #0 — foundation cleanup: `.gitattributes`, externalized Docker secrets, Jest scaffold, CI workflow, README. ✅
- Slice 0.5 — Zod v4 + next-intl wired (cookie-based locale, `src/i18n/request.ts`, `src/lib/i18n/locale.ts`, `NextIntlClientProvider`, bilingual toggle). ✅
- Slice #1 — Person CRUD (DB → API → UI → tests). ✅
- Slice #2 — Property CRUD with map view, Stereo70 input, PostGIS corners, bilingual UI. ✅
- Slice #2.5 — Property UI polish (Google Maps switch, draggable corners, click-to-draw, Stereo70/DMS input). ✅
- Slice #2.6 — Vercel + Supabase deployment + home page launching pad. ✅
- Slice #3 — Sidebar navigation refactor. ✅
- Slice #4 — Paperwork CRUD. ✅
- Slice #4.1 — Sidebar & nav polish. ✅
- Slice #4.2 — Paperwork filter re-sync + flag locale switcher. ✅
- Slice #4.3 — Inline field labels across all three detail forms. ✅
- Slice #4.4 — Document list filtering via sidebar checkboxes + "Paperwork" → "Documents" GUI rename. ✅
- Slice #4.5 — CI lint fixes: `react-hooks/set-state-in-effect`. ✅
- Slice #5.n — Relationships (People ↔ Properties ↔ Paperwork, self-refs), relationship map view. ✅
- Slice #6.0 — Pagination on all lists (page size 15, URL `?page=`). ✅
- Slice #6.1 — Pagination for association lists. ✅
- Slice #7.0 — Authentication (Supabase Auth, SSR cookie session, `src/proxy.ts` middleware). ✅
- Slice #7.1 — Bilingual (Romanian) sign-in page. ✅
- Slice #7.2 — Push auth to GitHub / Vercel. ✅
- Slice #7.3 — Fix TypeScript build error in `proxy.ts` (`cookiesToSet` typing). ✅
- Slice #8.0 — Principal Object base class + shared code sequence (`principal_object`, codes unique across all three types). ✅
- Slice #9.1 — Fix Romanian diacritics in lookup tables (`migration_009`, apply via `docker cp` + `psql -f`). ✅
- Slice #9.6 — Document Pages: file upload per record (`document_page`, local FS in dev / Supabase Storage in prod). ✅
- Slice #9.7 — Reference Data: split Services & Interests under an "Others" section. ✅
- Slice #9.8 — Reference Data: rename lookup table (`lookup_others`) + add Groups and Stamps. ✅
- Slice #9.9 — Reference Data: Description field on Services/Interests/Groups/Stamps. ✅
- Slice #10.03 — Reference Data: Person Roles list (`lookup_person_role`). ✅
- Slice #10.04 — Reference Data: Document Persons (`lookup_doc_type_person_role` junction). ✅
- Slice #10.05 — Reference Data: Property Persons (`lookup_property_person_role` whitelist). ✅
- Slice #10.06 — Role on Property ↔ Person association. ✅
- Slice #10.07 — Role on Document ↔ Person association. ✅
- Slice #12.01 — Judicial Person form refactor: Contact Persons panel (FK-linked natural persons) + consolidated Office Address ("same as" checkbox). ✅
- Slice #11.vercel.02 — Vercel/Supabase full reset + re-seed; fixed corrupted source files + stale judicial_person column refs. ✅
- Slice #GIS.13.02 — Add Property from scanned image: OCR (tesseract.js) + multi-step dialog. ✅
- Slice #GIS.13.03 — Add Property from text file / folder: `parse-text` route + batch import. ✅
- Slice #GIS.13.05 — Land map: auto-fit, red corner markers, drag-to-select batch delete. ✅
- Slice #GIS.13.06 — Land map: overlap-aware InfoWindow (all properties under cursor). ✅
- Slice #GIS.13.08 — Property form: Show Big Map toggle (two-column layout). ✅
- Slice #GIS.13.10 — OCR parser: fix first-corner-skipped (merged-token) bug. ✅
- Slice #GIS.13.11 — Text-file parser: first token any number < 1 000 + simple-polygon sort. ✅
- Slice #GIS.13.12 — Land map: select/unselect via InfoWindow + "Display all selected" tab. ✅
- Slice #15.01 — Admin → Import: local-folder browser + Classify (Property / Person / Document); ID-card vision extraction. ✅
- Slice #15.02 — Admin → Import: classified-state, context preservation, fit-to-panel preview, rotation, Word link, text preview. ✅
- Slice #15.05 — Project-wide rename: "Paperwork"/`PAPERWORK_TYPES` enum → "Document" + admin-managed `lookup_document_type`. ✅
- Slice #16.UX.01 — Person/Property/Document lists: most-recent-first sort + "New!"/"Nou!" recency badge. ✅
- Slice #15.06 — Reference Data: keep alternate Romanian wordings as distinct document types. ✅
- Slice #15.07 — Reference Data: `judicial_type` enum → admin-managed `lookup_judicial_person_type`. ✅
- Slice #15.09 — Sidebar nav cleanup + unified `/persons` list (Natural + Judicial). ✅
- Slice #15.09.1 — Delete orphaned `/natural-persons` and `/judicial-persons` list pages. ✅
- Slice #15.09.2 — Property sidebar: accordion → two flat-link buttons (List / Map). ✅
- Slice #15.10 — Property "Add new": no DB row on an untouched, all-blank create form. ✅
- Slice #15.11 — Reference Data Person panel: caption rename + button reorder. ✅
- Slice #15.12 — Reference Data: new "Roles" panel (Person Roles, Property Persons, Document Persons). ✅
- Slice #15.13 — Document form: "Show Big Page" toggle (mirrors Show Big Map). ✅
- Slice #15.14 — Document Pages viewer: wheel-zoom + click-drag pan in big-page mode. ✅
- Slice #15.15 — Ops: fixed-name, fully-regenerated `ciprian-schema-update.sql` (full wipe + rebuild every build). See Gotchas / UC-C6. ✅
- Slice #15.16 — Ciprian UAT bug fixes: `LOCAL_FILE_STORAGE` override, `ANTHROPIC_API_KEY` into compose, missing `update.bat`. ✅
- Slice #15.17 — Property corners: track original cadastral-file index per corner (survives reorder) + default display Stereo 70. ✅
- Slice #16.UX.02 — Help Content: Administration screen-help (Background + How-To) + inline micro-hints (`help_content`/`help_hint` + `src/lib/help/registry.ts`). ✅
- Slice #16.UX.03 — Properties Map: click/double-click InfoWindow model (replaces hover). ✅
- Slice #15.18 — Ciprian UAT bug fixes: text-file import 500 (Stereo70 grid file missing from Docker image; `original_index` column missing in Ciprian DB). ✅
- Slice #15.19 — Property Type & Use Category: drop `property_type`/`use_category` enums → nullable FK to `lookup_property_type`/`lookup_use_category` + form dropdowns. ✅
- Slice #18.01 — Property "Add new": enable Save as soon as any single field is entered (compute via `form.watch()`, not `getValues()`). ✅
- Slice #18.02 — Property versioning: full-snapshot history (`property_version`), version nav (◀ / label / ▶) on the corners-line, green/red label + per-field highlight frames + per-row corner red frames / removed-corner red line, derived by diffing adjacent snapshots; only the latest version is editable, edit-save stays on the property. ✅ (See "Versioning pattern & pitfalls" below.)
- Slice #18.03a — Properties Map: enable Street View Pegman (re-enable `streetViewControl` over `disableDefaultUI`; uses the map's built-in, non-billed default panorama). ✅
- Slice #18.03b — Property detail: Street View panorama panel keyed to a property's location (centroid of corners, free coverage check via `StreetViewService`, imperative `StreetViewPanorama`, lazy-loaded "Show Street View" toggle, bilingual). ✅
- Slice #18.05 — Person versioning (natural + judicial, one shared `person_version` table): full-snapshot history, version nav (◀ / label / ▶ + "Make current") centered on the person-name line, green/red label + per-field highlight frames (incl. address blocks via a new `AddressBlock` `highlights` prop). No corners. Only the latest version is editable; edit-save stays on the person. Shared pure diff core in `src/lib/versioning/field-diff.ts`; per-subtype helpers in each `_components/form-schema.ts`. ✅ (See "Versioning pattern & pitfalls" below.)
- Slice #18.06 — Document versioning (`document_version` table): full-snapshot history of the document's ~21 own fields (flat snapshot — no subtypes, no corners, no satellites), version nav (◀ / label / ▶ + "Make current") centered on the document-name line, green/red label + per-field highlight frames. Pages (uploaded files) and M:M associations are OUT of scope (separate lifecycles). Only the latest version is editable; edit-save stays on the document. Reuses the shared diff core in `src/lib/versioning/field-diff.ts` + the shared `VersionNavControls`; diff helpers in `documents/_components/form-schema.ts`. ✅ (See "Versioning pattern & pitfalls" below.)
- Slice #18.07 — Groups (Administration → Reference Data → Groups, now a dedicated screen, not the generic value-list modal). New `groups` + `group_member` tables (`migration_032`); supersedes the old `lookup_others` `category='Grup'` placeholder (deleted in the migration). A group has a single `target_type` (Physical/Judicial person, Property, Document), a required `description` (editable in the editor; ≤500 chars), and a system-assigned two-letter `code` (AA, AB … skipping **I** and **O**) drawn from `group_code_seq` and encoded in `src/lib/groups/code.ts` — codes are **never reused**. Members get a per-group `position` (`[02]`) allocated from `groups.last_position` (high-water counter) — **never reused** (removing leaves a gap). A property may belong to **at most 3 groups** (enforced server-side; candidates already in 3 other groups are filtered out). Editor: Area A (read-only target + code, editable description, "Add items" toggle) over two panels — B (available, search + checkboxes + "Add to Group") / C (in-group, checkboxes + position + "Remove from Group"); staged moves commit on "Save group" (server assigns positions, so a freshly-added member shows `[new]` until saved). Non-Property targets are creatable but their editor shows a "not implemented yet" notice. Property detail: up to three `[AA 02]` badges on the corners-panel header (from `listPropertyGroupTags`). Membership is NOT versioned (M:M, out of scope, like the other associations). Files: `src/lib/groups/{code,members,validation,queries}.ts`, `src/app/api/groups/**`, `src/app/admin/groups/**`. ✅
- Slice #18.10.diviz — Administration → Calculation: upload a 5-section data file (§1 big-polygon Stereo 70 corners / §2 orientation H or V — confirmed against the coords / §3 owners + percentages / §4 road corner SW·NW·SE·NE — the corner the road shares & starts from, and which belongs to owner 1 / §5 road width), compute each owner's parcel border (carve a shared road strip along one long side, then divide the rest by ownership fraction; EVERY inter-owner border is perpendicular to the road — a constant-u line — placed so each owner 1…N-1 gets its exact Final Area, and the last border between owner N-1 and owner N coincides with the road's perpendicular end cap as one straight line; owner N takes the full-height remainder so any percentage rounding lands on N). **The road's east end (where it meets owner N) is a right angle (perpendicular to the road's two parallel long sides); the road's west end just follows the polygon's existing slanted side. To make the right angle exact, the working frame is rotated to align u with the ROAD edge itself (not merely a long side), so both road long sides are constant-v and the cap at constant-u is truly perpendicular — keep this alignment as the feature is enhanced.** Preview on a Google map + areas table, then create one Property per owner (nickname = owner name) + an optional common-road Property and assign them all to a new PROPERTY-target Group. Scope: HORIZONTAL and VERTICAL polygons, all four start corners — the road runs along whichever long edge contains the Section-#4 corner (H: road on South/North side × start West/East; V: road on West/East side × start South/North); owner 1 is always at that corner, and the geometry runs in a frame rotated so u follows the road edge (so it is orientation-agnostic). Pure, unit-tested core: `src/lib/calculation/{geometry,parse,compute}.ts` (geometry is conversion-free Stereo 70 maths; compute converts to WGS84 via `transdatRO`). API: `src/app/api/calculation/{preview,commit}/route.ts`. UI: `src/app/admin/calculation/**`. ✅
- Slice #18.15.bugs — Versioning UX bug-fixes across Property/Person/Document forms. **(1)** The N-1 → N change (the latest save) is now pointed out: navigating onto the latest version from a different one — via the ▶ arrow or "Make current" — briefly PULSES the green/red diff frames on the changed fields (~3 blinks over ~2.5s, then clears). Reuses the existing `computeFieldHighlights` diff; the pulse is transient component state (`pulse` + a timer; a `pendingPulseRef` fires the post-"Make current" pulse once the refetched version arrives). Frames are delivered through the existing `highlight=` channel via a shared `FieldPulseContext` (`src/components/versioning/field-pulse.tsx`) + pure `highlightRingClass` (`src/lib/versioning/highlight-ring.ts`); the animation is `@keyframes ga-version-pulse-*` in `globals.css` (reduced-motion → steady ring). Property corner changes pulse a red ring on the whole corners card (the interactive latest-version table can't render the per-row historical diff). NOT pulsed after an ordinary edit-save (you just made that change). **(3)** Document Save now disables once the form matches the saved baseline (`saveDisabled = submitting || (mode==="edit" && isOnLatest && !editDirty)`), matching Property/Person — previously it was `submitting` only, so Save never greyed out after a save. **(4)** Audited the edit-dirty field-key lists in all four `formValuesEqual` (Property / Natural / Judicial / Document) — all complete; every form reads `form.watch()`, so there is no missing-field defect. The intermittent "non-address edit doesn't enable Save" is most plausibly the `!form.formState.isValid` gate keeping Save disabled while an unrelated/address field is invalid (correct behaviour). No backend/DB/i18n changes. ✅
- Slice #18.12 — Property "Street View address": a second street line on the single `property_address` row (`street_view_street_line`, `migration_034`), holding only the street/number/block/floor/apt portion derived from Street View — it shares the row's postal/locality/county/country, so those are entered once and never repeated. In the Address panel it sits under the document-derived street line with a "Fetch from Street View" button that reverse-geocodes the corners' centroid (`google.maps.Geocoder` on the lazily-loaded geocoding library) and fills `route + nr. + street_number`, editable afterwards. Versioned like any other address field (added to `PropertySnapshotAddress` + the snapshot build/keys + the address highlight-key lists — same green/red frames). Out of scope: still one address row (`country` stays `NOT NULL`, so the SV line only persists alongside a country); pre-existing version snapshots read the field as null (no backfill). Pure, unit-tested extraction core: `src/lib/geo/reverse-geocode.ts` (`streetLineFromGeocodeResult`). ✅
- Slice #19.09.Stamps — Stamps: list + create form (`/admin/stamps`), applicator (`/admin/stamps/[id]`) with target-type dropdown (all 4 types), staged changes per type committed together on "Save stamps", DB migration `migration_044_stamps.sql` (`stamps` + `stamp_member` tables, `stamp_code_seq`, STMP-AAA…ZZZ codes), bilingual i18n (`"stamp"` namespace). Value-list hub Stamps button → `<Link href="/admin/stamps">`. ✅
- Slice #20.01 — Entity "References" 5th tab (all four objects): second tab renamed "References" → **"Related"** (EN) / "Asocieri" (RO); new 5th tab **"References"** added to Natural Person, Judicial Person, Property, and Document detail pages, showing three sections: **Groups** (list of groups this item belongs to, or "not a member of any group"), **Stamps** (list of applied stamps, or "no stamps"), **Mentions** (work-in-progress placeholder). Backed by `listEntityGroupTags` + `listEntityStampTags` query helpers and three new `GET /api/{entity}/[id]/entity-references` routes. Shared `EntityReferencesTab` component (`src/components/entity-references-tab.tsx`). i18n: `shared.entityReferences` block + `tabs.related`/`tabs.references` in all four entity namespaces. ✅
- Slice #21.02 — Audit trail: `updated_by text` (email of last writer) added to `person`, `property`, `document`, `person_version`, `property_version`, `document_version` (`migration_054_updated_by.sql`). All six create/update query functions gained an `updatedBy` param; the six mutating API routes (`POST/PATCH /api/people`, `/api/judicial-persons`, `/api/properties`, `/api/documents`) extract `user.email` from the Supabase session and pass it through. Null for legacy rows and seed data. ✅
- Slice #19.30 — Soft-delete for all lookup/reference tables, groups, and stamps (13 tables: `lookup_property_type`, `lookup_tarla`, `lookup_use_category`, `lookup_person_type`, `lookup_person_role`, `lookup_judicial_person_type`, `lookup_citizenship`, `lookup_document_type`, `lookup_institution`, `lookup_property_property_role`, `lookup_document_document_role`, `groups`, `stamps`). `migration_057_soft_delete_lookups.sql` adds `deleted_at` to all 13. API DELETE handlers now soft-delete (set `deleted_at = NOW()`); all list/dropdown queries gain `WHERE deleted_at IS NULL`. M:M junctions keep ON DELETE SET NULL FKs but are never triggered (soft-delete doesn't cascade), so historical associations preserve their role tag. Whitelist junctions (`lookup_property_person_role`, `lookup_doc_type_person_role`, `lookup_person_person_role`) and all M:M entity junctions stay hard-delete. ✅
- Slice #20.06 — List-view metadata filters: Importance and Relevance filter dropdowns on all three entity lists (People, Properties, Documents) + "Expiring soon" toggle on the Documents list. Client-side local state (not URL params); WHERE clauses in all three query functions (`listPersons`, `listProperties`, `listDocuments`) + entityMetadata LEFT JOIN added to the corresponding count subqueries. New `shared.listFilters` i18n block (en-GB + ro-RO). No migration needed. ✅
- Slice #20.19 — UX housekeeping: **(1)** Page title / nav item / breadcrumb alignment — five mismatches fixed across `messages/en-GB.json` + `messages/ro-RO.json` (Reference Data, Tags, Help information, Global search, Users & Access). **(2)** "Others" panel (Groups / Stamps / Tags link buttons) moved from Reference Data hub to Settings page as the first panel; removed from `value-list-hub.tsx`, added to `settings-view.tsx` with `settings.sectionOthers` / `othersGroups` / `othersStamps` / `othersTags` i18n keys. **(3)** 10 hardcoded time thresholds made runtime-configurable: `time_frame_setting` table (`migration_063`), `src/lib/time-frames/config.ts` (types + `toMs`/`getDays`/`getMs`), `src/lib/time-frames/queries.ts` (server-side read/write), `GET`+`PATCH /api/time-frames`, `src/hooks/use-time-frames.ts` (React Query hook + `tfDays`/`tfMs` selectors). Consumers updated: dashboard query functions accept day-count params (injected from DB by `/api/dashboard`); `listDocument` accepts `expiringSoonDays` (injected by `/api/documents`); `EntityMetadataTab`, `NaturalPersonForm`, `DashboardClient`, `RecencyBadge` all call `useTimeFrames()`. Settings page gains a new "Intervale de timp" panel (editable number inputs per row, Save/Cancel, amber highlight on changed fields). Two-track i18n rule applies: static UI strings in messages JSON; DB label/description columns (`label_en`/`label_ro`, `description_en`/`description_ro`) are runtime content. ✅
- Slice #19.03 — Document form: type-specific fields + surveyor FK picker. **(1)** `subject` free-text field visible on all document types (between institution and title). **(2)** `date_valid_until` date field, shown when `showValidUntil` flag is set in `type-config.ts` (decisions, permits, authorisations). **(3)** `surveyor_id` FK → `person(id)` — a two-step picker dialog (Step 1: choose Natural/Judicial person type; Step 2: paginated name/code search list) with a link to the selected person's detail page; never a free-text box. **(4)** Seeds 4 new document types with Romanian diacritics: HOTARARE_JUDECATOREASCA, HOTARARE_ADMINISTRATIVA, DOCUMENTATIE_CADASTRALA, AUTORIZATIE_CONSTRUIRE (migration_035). DB migration: `migration_042_document_new_fields.sql` adds the 3 columns + backfills `document_version` snapshots. `getDocumentWithSurveyor` JOIN in `queries.ts` supplies `surveyorDisplayName`/`surveyorPersonType` to the form on page load. `type-config.ts` extended with `showValidUntil` + `showSurveyor` flags. ✅
- Slice #21.07.Import — Provenance value set + automatic assignment. **(1)** New 7-value list (`migration_067_provenance_values.sql`): `MANUAL` / `IMAGE` / `DOC_FILE` / `COORDINATE_FILE` / `ALGORITHM` / `AI_INTERPRETED` / `EXTERNAL_FEED`. `IMAGE_UPLOAD`→`IMAGE`, `EXTERNAL_IMPORT`→`EXTERNAL_FEED`, and `TEXT_FILE` **split** by `principal_object.object_type` (PROPERTY/DOCUMENT→`COORDINATE_FILE`, PERSON→`DOC_FILE`). The remap must hit **three** places — `entity_metadata.provenance`, `entity_provenance_log.method`, and the `provenance` key **inside `entity_metadata_version.snapshot`** — because "Make this version current" re-saves a stored snapshot, so a stale code there would fail the new CHECK at restore time long after the migration ran. No backfill of NULLs (Adrian's decision). **(2)** Single source of truth `src/lib/metadata/provenance.ts` replaces three hardcoded copies; the ones in `complex-query-view.tsx` and `global-search-view.tsx` had drifted to values (`SCAN_OCR`, `MANUAL_ENTRY`, `EXTERNAL_DB`, `OTHER`) that were never storable, so those two filter dropdowns could not match a single row. **(3)** Coded rules in `src/lib/metadata/provenance-rules.ts` (pure, unit-tested, 36 cases): coordinate file→`COORDINATE_FILE`, graphics file→`IMAGE`, PDF/DOC/TXT→`DOC_FILE`, AI extraction→`AI_INTERPRETED`, Calculation→`ALGORITHM`, Add-new form→`MANUAL`, unknown→`null` = **ask the user**. Keyed on the SOURCE alone, not target×source — the source is what determines provenance, and keying on it alone avoids combinatorial cases that all collapse to the same answer. `inferProvenanceForFiles` returns null for a mixed or unrecognised selection rather than picking a winner. **(4)** Applied server-side: the four create routes read an optional `provenance` off the request body (deliberately NOT added to the create Zod schemas — it belongs to `entity_metadata`, not the entity table, and those parsed objects go straight into `createX`) and write it via new `setInitialProvenance`, which is **best-effort by design**: the entity row is already committed by then, so a metadata failure must never turn a successful create into an error response. **(5)** `/api/documents/[id]/process` now also stamps the **created Property** with `COORDINATE_FILE` (before this slice only the source .txt document got a marker and the Property got nothing), and its own marker moved `TEXT_FILE`→`COORDINATE_FILE`. ⚠️ **Superseded by Slice #23.06.Import — do not act on the warning that used to be here.** It read: "That value doubles as the route's already-processed lock, so the 409 check, the claim UPDATE, the catch-block reset and `process-panel.tsx`'s matching check must always move together." True when written, and the source of a duplicate-Property bug: a value the rest of the app infers from a file EXTENSION cannot also be a mutex, because the import wizard legitimately writes `DOC_FILE` for a coordinate `.txt`. The lock now lives in `property_corner_source` and provenance is metadata again. **(6)** UI: shared `ProvenanceField` (read-only badge when a rule fires, required select when not) in the Document / Person / Property classify panels; `BulkImportDialog` gained a **pre-import gate** listing only the entries whose extension it cannot classify (with "Apply to all") — when everything is inferable, the normal case, the gate never appears and the import still starts on mount as before. **(7)** i18n: `shared.entityMetadata.provenance.*` labels + `statement*` sentences rewritten in both languages, `globalSearch.provenanceValues` re-keyed to the real codes, new `adminImport.provenance` block. The statement map in `entity-metadata-tab.tsx` is now derived from the shared list rather than hand-listed, so a new code cannot ship without its explanatory sentence. ✅

- Slice #21.09.help.error — Help screens returned 500 (`relation "help_content" does not exist`). **Root cause was not the missing table but the tracker:** `migration_026_help_content.sql` was never applied locally, yet `migration_056_schema_migrations.sql` backfilled `schema_migrations` for 008–055 **by assertion, not by inspection** — so 026 was recorded as applied and `Apply-Migration.ps1` reported "up to date" forever. Self-concealing and permanent. **(1)** Applied 026 locally + rewrote its `schema_migrations` row with a real MD5. **(2)** New `scripts/Verify-Schema.ps1` — read-only drift detector: parses every `pgTable("…")` out of `src/db/schema/index.ts`, diffs against `information_schema.tables`, exits non-zero on drift. `Apply-Migration.ps1` now prints a warning that "up to date" only means *recorded*, not *present*, and points at it. **(3)** Audit found `src/db/supabase_schema_full.sql` far worse than expected: **37 of 49 tables, and 21 of those 37 missing columns** — frozen around Slice #18.07 (`group_member` still had `property_id` instead of `principal_object_id`). Any DB rebuilt from it was born broken. Replaced hand-maintenance with `scripts/Export-SupabaseSchema.ps1`, which regenerates it from the live local DB via `pg_dump` (aborts if migrations are pending or `Verify-Schema` finds drift; handles the UTF-16LE and `CREATE SCHEMA topology` gotchas). **(4)** New `src/db/supabase_repair_missing_tables.sql` — additive, idempotent, safe on live data: creates the 12 absent tables **in their FINAL shape** (not the historical migration shape — 051/046/047/050/057/067 all reshape `stamps`/`stamp_member`/`entity_metadata` after creation) plus every drifted column, with pre/post-flight `RAISE NOTICE` reports. Deliberately does *not* attempt `group_member`'s 051 transition or drop `document.institution` — those are data migrations, so it warns instead. No application code, i18n or schema/index.ts changes. ✅

- Slice #21.11.uat.auth — Ciprian's UAT box showed "Sesiunea a expirat. Vă rugăm să vă autentificați din nou" on **Procesează**, on a build whose login link is deliberately hidden. `UAT_NO_AUTH=true` was honoured in only four places (`middleware.ts`, `admin/layout.tsx`, `api/auth/me`, `lib/storage`) while **25 files called `supabase.auth.getUser()` directly**; `/api/documents/[id]/process` hard-failed with 401 and `process-panel.tsx` maps 401 → "session expired". The message was right about the status and wrong about the cause. **(1)** New `src/lib/auth/current-user.ts` — `getCurrentUser()` / `getCurrentUserId()` / `getCurrentUserEmail()` / `isUatNoAuth()`, returning a synthetic `uat-no-auth` identity (stable id for the OCR rate-limit bucket, **null email** so `updated_by` records no author rather than a fake one). Never throws: an unreachable Supabase project resolves to null so a route degrades to 401 instead of 500. **(2)** All 25 call sites converted; `updatedBy` fallbacks simplified from `email ?? id ?? null` to `email ?? null` (an opaque uuid in `updated_by` was never useful). `admin/layout.tsx` keeps an explicit `isUatNoAuth()` early return — the synthetic identity has no `app_users` row, so the role lookup below it would redirect every admin page to `/`. **(3)** Users & Access hidden from the sidebar in UAT mode (it approves sign-ups via the Supabase Admin API, which does not exist there); `admin/users/page.tsx` still bounces a hand-typed URL. **(4)** `src/__tests__/auth-single-source.test.ts` fails the build on any `supabase.auth.getUser()` outside the helper, or any direct `process.env.UAT_NO_AUTH` read inside `src/`. No DB, migration or i18n changes. ✅

- Slice #21.10.Import — **Discover mode**: a second, schema-free reading mode on `POST /api/documents/[id]/ai-interpret`, selected by an optional `{ "mode": "discover" }` body (a bodyless POST — what every existing caller sends — keeps the extract path byte-for-byte unchanged). **Why it exists:** `buildExtractSystemPrompt` is schema-DRIVEN — it hands the model a target field list (4 generic baseline fields + the type's `template_fields`) and offers `unmappedRaw` as a leftovers channel. For a type the system does not understand yet there is no template, so the model is told to find four fields and dump the rest into one flat map — and because that map is framed as leftovers, it self-censors, answering "what else is printed?" rather than "read me this document". Discover mode gives it **no target fields at all** and asks for two lists: `recognised[{name,value,confidence}]` (every label→value pair, **both sides VERBATIM** — no translation, no canonical renaming, no date/number reformatting, because the printed Romanian label is the evidence for what a future template field should be called) and `sections[{heading,lines[]}]` (everything else, grouped by the model's own inferred headings, in document order). **Persists nothing** — no field mapping, no `customFields`, no `documentTypeId` resolution (so unlike the extract path it never auto-creates a `lookup_document_type` row), no party extraction, no `ai_interpreted_at` stamp; safe to re-run on any document, any number of times. Output goes to the terminal running `npm run dev` via a pure, unit-tested formatter (`src/lib/documents/discover-log.ts`, 41 assertions) and is mirrored in the response body, following the precedent Slice #21.02.Import set when it stopped making these diagnostics console-only. Three deliberate additions beyond the console dump: **(a)** pages that cannot be sent are now collected into `skippedPages` with a per-page reason and reported — previously an unsupported page was dropped with only a code comment, making "the model found nothing" and "the model never saw this page" indistinguishable from outside (the `application/octet-stream` case is called out by name: the file is fine, only its recorded MIME is missing); the existing 422 now carries `skippedPages` too. **(b)** `max_tokens` is 16384 in discover mode (vs 8192) because verbatim output scales with document length, and `stop_reason === "max_tokens"` is surfaced as a loud TRUNCATED warning — a silently partial read would be the worst possible failure for a mode whose entire promise is completeness. **(c)** `parseDiscoverPayload` never throws (same contract as `parseTemplateFields`) — a diagnostic that crashes on odd data fails exactly when the data is odd. UI: a "Descoperire AI" button beside AI Interpret, deliberately NOT gated on `aiInterpretedAt` (it writes nothing, and re-running after adding template fields to see what is still unrecognised is a primary use case); both buttons lock while either runs, since they share a route and a rate-limit bucket. Console report is English (dev diagnostic, sits beside the existing English `[ai-interpret]` block); the document's own content inside it stays Romanian and verbatim. No DB, migration or schema changes. ✅

- Slice #19.28.e2e — **Playwright end-to-end suite** (first one in the repo). `playwright.config.ts` + `e2e/`: an `auth.setup.ts` project that logs in through the real `/login` form, pins `NEXT_LOCALE=ro-RO` and creates/reuses one fixed "E2E Proprietate Test" property (uuid cached in the gitignored `e2e/.auth/e2e-ids.json`), then a `chromium` project that runs every spec from that saved session. `workers: 1` / `fullyParallel: false` on purpose — all specs share that one property row. First suite is `e2e/versioning/property-versioning.spec.ts`: four tests over save→version-append, ◀ read-only navigation, "Setează ca actuală", and Save's dirty-state tracking (the Slice #18.15.bugs class). Assertions are all RELATIVE (`startVersion + 1`) so the accumulating version history on the shared row never invalidates them. Locators live in `e2e/helpers/version-nav.ts` and match **Romanian** strings from `messages/ro-RO.json` — renaming a Romanian UI string breaks these tests by design. `npm run e2e` / `npm run e2e:ui`; needs a running `npm run dev`, `E2E_EMAIL`/`E2E_PASSWORD` in `.env`, and a one-off `npx playwright install chromium`. **Deliberately NOT in CI** — CI has no Postgres, dev server, seeded user or browser binaries, and the config has no `webServer` block; wiring that up is its own slice. Jest already excludes `e2e/` (`testPathIgnorePatterns`, committed earlier in 60d0d81). Full operating guide in `e2e/README.md`. Two landing fixes folded in: the `Dockerfile` deps stage was reverted from `npm install --ignore-scripts` back to `npm ci` (see the Gotcha below — `--ignore-scripts` silently drops the pdfjs worker), and `.dockerignore` gained `e2e` / `playwright.config.ts` / `playwright-report` / `test-results` so the live Supabase session cookie in `e2e/.auth/` never enters the build context. **A third, more substantial fix**: Slice #20.12 (shipped after this suite was originally written) added a compact "history discovery chip" that replaces the full ◀ / "v N" / ▶ strip whenever a property is viewed at its latest version and has ≥1 prior version — the chip shows a total version *count*, not the current number, so `getByText(/^v \d+$/)` genuinely found nothing once the fixed E2E property accumulated a second version, in every browser, not just under Playwright. Fixed at the source rather than in the tests: `src/components/version-nav-controls.tsx` now renders a visually-hidden (`sr-only`) "v N" span alongside the chip — no visible change for sighted users, and it closes a real latent accessibility gap (a screen-reader user on latest previously had no way to hear which version they were viewing, only the total count). ✅

- Slice #22.06 — **Superseded by Slice #23.04.Import**, which deleted the route and every component it exposed; this entry is history only. Re-exposed the orphaned original Import flow (`ImportBrowser` + `ClassifyDialog` + `FolderScanDialog` + the three classify panels, ~2,600 lines, see the "unreachable" gotcha above) at a new route, `/admin/import-legacy`, with its own nav entry beside the live "Import în sistem" item — on request, purely so it can be looked at for reference. No changes to any of that component code; only a new page (`src/app/admin/import-legacy/page.tsx`, with an inline banner explaining what it is and that some fields — notably provenance — are stale relative to the current schema), its own `ssr:false` dynamic wrapper (a copy of the live wizard's, kept in a separate file so the active route's wrapper is untouched), a nav-config entry, and i18n keys. `src/lib/help/route-map.ts` gained `/admin/import-legacy` in `HELP_OPTED_OUT` — the `help-coverage` Jest gate fails the build on any new route with no help decision, and this screen's own inline banner already explains it, so a formal Background/How-To pair would be redundant. ✅

- Slice #23.00.Import — **One picked folder = one Property.** The import wizard's folder semantics changed: a picked root folder no longer describes an arbitrary tree of documents tagged by ancestor folder names — it IS exactly one Property, named explicitly by the user. **(1)** New `property` phase between `ready` and `tag-dialog` (`PropertyStepDialog`, `src/app/admin/import/_components/property-step-dialog.tsx`): pick an existing Property (search list over `GET /api/properties`, modelled on `SurveyorPickerDialog` from Slice #19.03) or create one now with the nickname pre-filled from the folder name. The import cannot start without a resolved Property; the wizard shows it as a chip in the toolbar and clears it whenever a new folder is picked. **(2)** Coordinate files are shortlisted by extension (`.txt/.csv/.dat/.asc`) and then **actually parsed** via the existing `POST /api/properties/parse-text`, so the list shows real corner counts — a `.csv` of phone numbers and a `.csv` of corners are indistinguishable by name, and "0 corners" is the only thing that tells them apart. Exactly one usable candidate is pre-selected; **two or more and the user must choose**, because "most corners wins" would let a stray CSV take it silently. **(3)** On a NEW property the corners travel inside the `POST /api/properties` body so they land in **version 0** — creating first and patching after would open the property's history with an edit nobody made. On an EXISTING property with corners already present, a replace/keep prompt shows both counts before a `PATCH` appends a normal version; replacing is destructive (it discards manual corner reordering — the bow-tie fix) so it is never the default. **(4)** Every Document created by the run is linked to the Property **directly** (`associateDocumentsWithProperty`, right after `createDocument` and *before* the tags, so a later failure still leaves the document attached to the right property). That helper was previously fire-and-forget on the dead AI branch; it now throws on `!res.ok` and carries the `res.redirected` expired-session guard, since a silently dropped link is the exact failure this slice exists to prevent. **(5)** The digit-prefix heuristic is **retired from the wizard**: `folderInfo` is gone from `FSFileEntry`/`FSPageGroupEntry`, `walkFolder` lost its `ancestorInfo` third parameter, and `scan-table.tsx` lost the tarla/parcela column — it now groups by any first-level subfolder (purely structural) and renders folder names verbatim (`perToSlash` was dropped from the display: on an arbitrary name it turns "Personal" into "/sonal"). **`parseFolderName` itself survives for two server-side consumers — see the gotcha below.** **(6)** Tags still exist and are still applied, but they are **descriptive only**; they link nothing. **(7)** Pure, unit-tested core: `src/lib/import/coordinate-file.ts` (`coordinateCandidates`, `nicknameFromFolderName`, `isCoordinateFileName`) — the nickname helper deliberately does NOT expand abbreviations the way `folderNameToTitleHint` does, and decodes nothing cadastral. No DB, migration, or help-registry changes (`property_document` already had the right shape; no new route). ✅

- Slice #23.01.Import — **ID-card extraction ported into the live Import Wizard, with real person matching.** The extraction route (`extract-id-card`) is unchanged. What was ported is the *surrounding* UX from the orphaned `person-classify-panel.tsx`, minus its defect. **(1)** The row action lives on a **completed row of the import results table**, not on the pre-import scan table — by then the bulk import has already created the Document, uploaded the image as page 1, and linked it to the run's Property, so the person flow only resolves an identity and attaches it. Doing it pre-import would create a **second Document for the same image**, since the wizard imports every entry unconditionally and has no skip mechanism. **(2) The defect that is fixed:** `PersonClassifyPanel` calls `POST /api/people` unconditionally — no CNP check, no fuzzy fallback, no decision — so re-importing the same card, or one OCR slip in a name, silently creates a duplicate person. It now goes through the same confirm-or-create ladder as the document-party path: exact `findNaturalPersonByCnp` → fuzzy `searchPersonsAll` labelled *unconfirmed* → explicit confirm / pick / create-new / skip. **(3)** The four branches were lifted out of `ai-party-linker-dialog.tsx` into a shared `src/components/persons/person-resolution-dialog.tsx` (549→284 lines for the original caller, public contract unchanged) rather than copied — a second copy of the one safety net that stops two real people being merged is exactly the "fourth call site gets missed" failure recorded for `UAT_NO_AUTH`. `isCreateBranch()` is exported so the ID-card caller renders its review form under precisely the branch the dialog renders. **(4)** New `POST /api/admin/import/resolve-natural-person` supplies the matching half, which `extract-id-card` never had (the party path resolves server-side inside `ai-interpret` before its dialog ever mounts). It writes nothing. It tries the full name, then falls back to **surname alone** — `person.displayName` is `"firstName lastName"` and `searchPersonsAll` is a single `ILIKE %pattern%`, so a middle name or a surname-first record misses entirely, and a missed suggestion is how a duplicate gets created. **(5)** The review form is editable **before** `POST /api/people`, deliberately: `natural_person.cnp` is immutable once written (`migration_025` trigger), so a misread digit fixed here is free and fixed later is a data migration. **(6)** Links: `property_person` to the #23.00 Property, then `person_document` to the existing Document — property first, so a failure still leaves the person on the right property. Both `personRoleId: null` — an ID card says nothing about the holder's role on the land. **(7)** Person provenance is `AI_INTERPRETED` and never asked (every field came from the model's reading, even the corrected ones); the Document keeps whatever the import's provenance gate assigned it. **(8)** `isIdCardEntry` (pure, unit-tested, `src/lib/import/id-card.ts`) replaces a raw `description.toLowerCase().includes(...)`: it prefers Haiku's `typeKey`, lets a *confident non-ID* key veto the label, folds Romanian diacritics via NFD (covering both the comma-below **ș/ț** and the legacy cedilla forms some OCR emits), and vetoes **"carte de identitate a vehiculului"** — a real document (CIV) that the old substring match would have fed to the ID-card extractor. **(9)** `KNOWN_TYPE_KEYS` re-synced: six keys removed. `CARTE_IDENTITATE_ALT` was never seeded (`migration_021` defines the only three real alternate wordings), so an ID card classified under it auto-created a type whose generated key `getPersonIdCardLink` — which matches the literal `CARTE_IDENTITATE` — could never find. `CASA`/`LINIARA`/`PASUNE`/`TEREN_ARABIL`/`TEREN_CONSTRUIT` are `lookup_property_type` keys (`migration_039`) that were inviting the model to classify a *document* as a kind of land parcel. **(10)** i18n contract test (`person-resolution-keys.test.ts`): the shared dialog takes its `t` from the caller so it cannot type-check its own keys — the test asserts all 29 `RESOLUTION_KEYS` plus placeholder parity across both namespaces × both locales, which is the class of bug the bulk-delete dialog shipped with from Slice 15.03. **No DB, migration, or schema changes.** ✅

- Slice #23.02.Import — **The other two dead AI actions from the orphaned wizard, ported for real.** ID-card extraction shipped in #23.01; this slice replaces the wizard's other two dead offers (coordinate-file→Property, generic-document→field-update) and removes the scaffolding that used to (not) drive them. **(1) Coordinate file → Property corners.** No new parser: reuses the existing `POST /api/properties/parse-text` (the same route #23.00's `PropertyStepDialog` already calls) via a new `CoordinatePropertyDialog`. There is no merge — `PATCH /api/properties/[id]` is replace-all and a corner list is an *ordered* polygon, so the only honest choices are replace or leave alone. Four outcomes are decided before the user is asked anything: 0 parsed corners → say so, stop; identical to the Property's current corners → "already applied", nothing written (no version burned); Property has none yet → confirm-and-write; Property has *different* corners → #23.00's exact replace/keep prompt (Păstrează default), because replacing discards any hand-fixed corner order (the bow-tie case). The identity check is a new pure `cornersEqual` (`src/lib/import/coordinate-file.ts`, epsilon `1e-9`°) — **order-significant** (a reorder is a real edit, not a no-op — it's how the bow-tie fix works) and **ignores `originalIndex`** (provenance metadata, not geometry). **(2) Generic document → field update.** Does NOT port the orphaned wizard's own fixed `EXTRACT_SYSTEM_PROMPT` (`classify-prompts.ts`'s flat ~18-field list). Calls `POST /api/documents/[id]/ai-interpret`'s existing extract mode instead — per-document-type template-aware, and it reads all of the document's pages from storage itself, so the action needs nothing but a `docId` (no client-side PDF rasterisation). Writes fields + `customFields` + appended Enhanced Notes + `aiInterpretedAt` in **one** PATCH — the orphaned code sent two, which on a versioned entity meant two `document_version` rows for one click. A type reclassification the route returns (`fields.documentTypeId`) **is applied** — Adrian's call: a model reading every page is better evidence than the import scan's thumbnail glance, even though it's the same path that can auto-create `lookup_document_type` rows on unfamiliar folders (see the gotcha above); accepted deliberately, to be reviewed/consolidated in Reference Data after import rather than gated here. Extracted parties go to the existing, unmodified `AiPartyLinkerDialog` — linked to the **Document only**, never the run's Property (unlike #23.01's ID-card path): a Notar or Mandatar doesn't belong in `property_person`, and the Document is already attached to the Property. **(3) Scaffolding removed** as the real replacements went in: `AiPhase`, `AiState`, the callerless `_handleAiInterpret`, the 137-line `AiPanel`, and their three handlers (`handleCreateProperty`/`handleCreatePerson`/`handleExtractFields`) plus `createProperty`/`extractDocumentFields`/`createNaturalPersonFromFields` and a duplicate `TEXT_EXTS_SET`/`isTextFile` pair — `isCoordinateFileName` from #23.00 is now the one place "which extensions might hold corners" is decided. `ImportWizard` gained an `onPropertyCornersChanged` callback so the toolbar's corner-count chip stays honest after a row replaces the Property's corners mid-run. No DB, migration, or schema changes. ✅

- Slice #23.03.Import — **Hardening bundle carried forward from the earlier workflow-risk review — most of which turned out to be already fixed.** The slice as briefed had six items; investigation against HEAD found **four already shipped**, and the code even carries the review's own numbering in its comments (`fix 7.6`, `(7.8)`, `fix 7.9`, `fix 7.11`, `issue 7.1`, `issue 7.5`) because those items were fixed as they were found rather than deferred. **Already done, verified not re-done:** (1) Stereo70 `parseLine`/`isStereo` extraction — `src/lib/geo/stereo70-parse.ts` already existed and both routes already imported it (the #23.00/#23.02 dialogs never needed it: they call `POST /api/properties/parse-text` over HTTP and duplicate no parser). (3) The `res.redirected` session guard was already on the wizard's document-create, page-upload **and** property-association calls, with a `session-expired` sentinel handled in the import loop. (5) `isSystemFile` already covered dot-prefixed files plus the five Windows names, `walkFolder` already filtered through it before `isPageGroup`, and `folder-utils.test.ts` already pinned the "10 scans + Thumbs.db" case. (6) Abbreviation matching was already case-insensitive via a `gi` regex. (2) was **substantially** done: `/api/documents/[id]/process` already serialises concurrent callers with an `INSERT … ON CONFLICT DO NOTHING` + `SELECT … FOR UPDATE` provenance claim, and already uses a compensating-action pattern instead of a transaction — deliberately, because `createProperty` runs its own internal transaction for the version snapshot and an outer `tx` would have to be threaded through the whole properties query layer. **The transaction rewrite was therefore dropped from this slice**, and the briefed goal it was meant to serve was re-read: a DB transaction cannot prevent duplicate-property creation on a rapid triple-click at all, because each click is a separate HTTP request and therefore a separate transaction — all three commit happily. That is a client concern, and it shipped as (5) below. Provenance stays **outside** any transaction, preserving the #21.07.Import decision that a metadata failure must never turn a successful entity create into an error response. **What actually shipped, five commits:** **(1)** The only fully-outstanding briefed item — scan confidence (`high`/`medium`/`low`) reaches the AI actions. The coloured badge in the pre-import scan table already existed (7.8); what was missing is that `IdCardPersonDialog` and `DocumentAiInterpretDialog` received no confidence at all, so by the time a user reviewed extracted values there was no reminder the scan was unsure what the document even was. New shared `ScanConfidenceWarning` (`src/app/admin/import/_components/scan-confidence-warning.tsx`) — amber `role="status"` for medium, red `role="alert"` for low, nothing at all for high (a banner on nearly every row trains the user to ignore banners). It **owns its own i18n namespace** (`adminImport.wizard.scanConfidence`) rather than taking a `t` from the caller, so its keys type-check in one place instead of needing the contract test `person-resolution-dialog.tsx` required in #23.01. It renders in **every** phase of the AI-interpret dialog including `done` — a finished extraction is precisely when the result is most likely to be accepted. It matters most on that path because the route builds its prompt from the document TYPE's `template_fields`, so a mis-classified document is asked for the wrong fields entirely and comes back looking just as complete as a correct one. Confidence is read at the render site from `scanResults.get(target.path)?.confidence`, not copied onto the target state, so there is no second copy to keep in step. **(2)** Abbreviation matching now also folds Romanian diacritics. **The `\b` finding is the reusable part — see the gotcha below.** Keys are expanded into per-letter character classes covering both encodings of ș/ț, regex metacharacters are escaped (defensive; no current key has one), and the space in a multi-word key becomes `\s+`. **(3)** `src/__tests__/stereo70-parse.test.ts` — the extracted parser was the one pure module in `src/lib/` with no test file. Writing it surfaced a **documentation defect**: the module claimed "Decimal separator: dot or comma (both normalised to dot before parsing)". False. Comma is in the delimiter class, so `"321762,117"` splits into `"321762"` and `"117"` before the `.replace(",", ".")` is reached — that replace only ever sees single tokens, which by then cannot contain a comma, making it dead code. A comma-decimal line is **rejected** (`null`), not misread, so no wrong coordinate is ever stored — but such a file imports as zero corners. **Left as-is deliberately**: the two readings conflict, since `"1,321762.117,584000.250"` is a real comma-DELIMITED row that parses correctly today and would break. Comment corrected, limitation pinned by test so it cannot regress into a misread; supporting both needs a per-file separator decision, which is its own slice. **(4)** `src/lib/calculation/parse.ts` had a **third**, byte-identical private `isStereo` that item 1's original consolidation never knew about; it now imports the shared one. **(5)** `property-step-dialog.tsx` gained a `useRef` in-flight latch on Confirm. `submitting` state alone was not enough: setting state does not disable the button until React re-renders, so clicks dispatched inside that window still pass the `disabled={!canConfirm}` gate and still run the handler — on the "new property" branch each is a separate `POST /api/properties` with nothing server-side to deduplicate them, so a triple-click produces three real Properties sharing a nickname and the run's documents attach to whichever answered last. The ref flips in the same synchronous turn as the click; it is released only on failure, so a success leaves it closed while the dialog unmounts. **No DB, migration, schema, route or help-registry changes.** ✅

- Slice #23.04.Import — **The orphaned original Import surface is deleted.** Slices #23.01/#23.02 ported everything worth keeping out of the pre-#21.01 `ImportBrowser` flow (ID-card extraction, coordinate-file → Property, generic-document → field update), so the originals had no remaining purpose, and Slice #22.06's `/admin/import-legacy` route existed only to keep them visible for reference while that porting happened. **3,229 lines removed, 31 added (net −3,198), plus 90 i18n keys × 2 locales** — counted from the diff, not estimated. **What went:** the six-component cluster (`import-browser.tsx` 605, `folder-scan-dialog.tsx` 745, `classify-dialog.tsx` 166, `person-classify-panel.tsx` 670, `property-classify-panel.tsx` 166, `document-classify-panel.tsx` 253) and the whole `src/app/admin/import-legacy/` tree (page 48 + its own `ssr:false` wrapper 31) — 2,684 lines; then the orphans that fell out with them — `file-system-types.ts` (33; the wizard uses the types in `src/lib/import/folder-utils.ts` instead), `resolveProvenance` in `provenance-field.tsx` (⚠️ the component itself SURVIVES — `bulk-import-dialog.tsx` and `id-card-person-dialog.tsx` both render `ProvenanceField`), `POST /api/admin/import/extract-document` (192 — a live, unreachable, Anthropic-billing endpoint once its only caller went) and its `EXTRACT_SYSTEM_PROMPT` (⚠️ `CLASSIFY_SYSTEM_PROMPT` and `KNOWN_TYPE_KEYS` in the same file STAY: `scan-folder` and `ai-interpret` both use them). Then the nav entry plus its now-unused `Archive` icon import, the `sidebar-nav.tsx` label, the `/admin/import-legacy` entry in `HELP_OPTED_OUT`, and in both locales the `adminImport.browser` (18) / `classify` (42) / `scan` (27) blocks + `nav.items.importLegacy` + `adminImport.legacyPageTitle` / `legacyBanner` (`pageTitle`, `provenance` and `wizard` all stay). **Three help hints had to go or the build breaks:** `ctrl-click-multi-select`, `ocr-extract` and `import-leave-warning` rendered ONLY in the deleted files, and `src/__tests__/help-coverage.test.ts` invariant 2 fails on any registered hint with no `<HelpHint>` placement — so they are out of `src/lib/help/registry.ts` (the `admin-import` help SCREEN stays; it serves the live wizard). 📝 Their Romanian text lives in the `help_hint` DB table, so this orphans three rows rather than deleting any content — they can be re-registered against the wizard later. **`ImportWizard` is now the only Admin → Import surface.** ⚠️ `src/app/admin/import/_components/import-browser-dynamic.tsx` (37 lines) is NOT part of the deletion — despite the name it is the live wizard's `ssr:false` wrapper, imported by `src/app/admin/import/page.tsx`; only its stale comments were corrected, along with those in `id-card-person-dialog.tsx`, `document-ai-interpret-dialog.tsx`, `classify-prompts.ts`, `lib/rate-limit/ocr.ts` and `api/documents/[id]/ai-interpret/route.ts`. No DB, migration, schema or behaviour change — nothing user-reachable renders differently, and no Romanian string on a live screen was touched. ✅

- Slice #23.05.UX — **Global button enabled/disabled states.** Adrian's UAT report: "the action buttons like Import or Close are white and the only difference between disabled and enabled is the label, which is a little bit fading for disabled". Cause: `disabled` was expressed everywhere as `disabled:opacity-*` layered on top of the enabled style, which MULTIPLIES the enabled appearance rather than replacing it — 50% of "white with a grey border" is still white with a slightly greyer border. **The state rule, in one sentence: disabled = white/neutral surface with a `border-wire` edge and `text-fade` label and no colour fill, identical for every variant; enabled = that variant's full strong colour.** **(1)** One shared source of truth: pure `buttonClass({ variant, size, pill?, className? })` in **`src/lib/ui/button-styles.ts`**, with six variants — `primary` (Save / Import / folder picker), `secondary` (Close / Cancel), `danger` (delete), `ghost` (inline chips), `bare` (surface-less glyph buttons, e.g. the version-nav ◀ ▶) and `bare-danger` (surface-less destructive text links) — and four sizes `xs`/`sm`/`md`/`lg`, each transcribed from a class string that already existed so no button was silently resized. Chosen as a FUNCTION, not a `<Button>` component: a `className=` swap is a one-attribute edit that cannot alter the `disabled` attribute several e2e assertions depend on, whereas a component swap rewrites JSX structure in 67 files. Precedent: `highlightRingClass` in `src/lib/versioning/highlight-ring.ts` is already a pure class-string builder. **(2) It takes no `disabled` boolean** — the string carries BOTH states and lets CSS pick via `:enabled`/`:disabled`. A boolean would duplicate the `disabled={...}` prop at 314 call sites and the two would eventually drift, leaving a button that is disabled but painted as enabled. **(3) Every hover rule is `enabled:hover:`, never bare `hover:`** — a disabled `<button>` still matches `:hover` in every browser, so a bare hover rule repaints an inert button on mouse-over; it also sidesteps Tailwind variant-ordering entirely, since `:enabled:hover` and `:disabled` are mutually exclusive selectors. **(4)** New `--color-danger` / `--color-danger-d` tokens in `globals.css` (#B91C1C / #991B1B — desaturated to sit with the slate palette; white label measures 6.5:1). **(5) Dark mode**: `bg-cta` (#334155) on a zinc-900 page is a **1.69:1** contrast, so an enabled dark-mode primary in the light-mode fill would be invisible — primary lightens to `slate-400` (6.9:1 against the page, 7.1:1 for its slate-900 label). Danger stays red rather than inverting; an inverted red reads as pink and stops signalling "destructive". Note the app has **no dark-mode toggle** — `dark:` resolves purely off `prefers-color-scheme`. **(6) Focus rings**: `grep 'focus-visible:' src/` returned **zero** hits before this slice, so no button in the app was visible to keyboard focus. The helper's base adds `focus-visible:outline-*` using the existing `--color-focus` token (`outline`, not `ring`, so it needs no ring-offset background colour and renders correctly on canvas, in a white card and on dark alike). **(7) Scope grew twice during the work, both deliberately.** First, the briefed "67 files / `disabled:opacity-50|40`" undercounted: the real figure was **193 occurrences across 68 files**, because `-60` (10, the login/signup/change-password forms) and `-30` (5, version-nav + corners-manager) were never in the search. Second — and the one that mattered — only 193 of the app's **356** buttons carried a disabled state at all; the rest never did. That was invisible while `secondary` meant "white with a wire border", but `secondary` enabled is now a pale-slate fill, so a converted button no longer matches an unconverted neighbour. The corners table row would have shipped as `[↑][↓]` pale-slate beside a plain-white `[Editează]`. Adrian's call: **convert every button in every file the slice touches**, so no row is ever half-converted. Final state: **314 `buttonClass` call sites, 67 files changed, 193 → 6 remaining `disabled:opacity-*`**, all six on non-button native controls (3 row-select checkboxes, 3 `<select>`s) and allowlisted by name. **(8)** `PaginationControls`' `const BTN` — the codebase's only pre-existing attempt at a shared button string — is now just a call to the helper. **(9)** Deliberately NOT converted: the `text-navy` "Înapoi la listă" back-navigation buttons (a distinct tokenised navigation affordance, never disabled) and `process-panel.tsx:289`'s emerald already-processed indicator, which carries a RAW `opacity-50` rather than a `disabled:` one and is a status badge rather than an action. **(10)** Enforcement: `src/__tests__/button-styles-single-source.test.ts` fails the build on any `disabled:opacity-` outside the allowlist, asserts the allowlist contains no `<button>`, and asserts every allowlist entry is still live so a stale exemption cannot outlive its reason. The regex is deliberately open-ended (`disabled:opacity-`), because a value list is exactly what made the original inventory miss 15 occurrences. No DB, migration, schema, route, i18n or behaviour change; no label, `disabled` attribute or `aria-disabled` semantic was touched, so the Romanian e2e locators in `e2e/helpers/version-nav.ts` are unaffected by construction. ✅

- Slice #23.06.Import — **The coordinate-document → Property link, and the duplicate-Property bug.** Adrian imported a property folder, used the results-table row action to turn the coordinate `.txt` into the run's Property, then opened the same document from the Documents list and found the Process panel still enabled. Using it created a **second Property with identical coordinates** — visible on the map only as a flicker where the two polygons overlapped. **Root cause:** `provenance` was doing duty as a concurrency lock. `classifyFileSource()` maps a file by extension alone and deliberately never returns `COORDINATE_FILE` (a `.txt` is indistinguishable from any other text file by name); `"txt"` is in `DOCUMENT_EXTENSIONS`, so the wizard stamped `DOC_FILE`; `process-panel.tsx` tested `prov === "COORDINATE_FILE"` and read that as *not processed*. Both subsystems were correct on their own terms — which is exactly why it survived. CLAUDE.md's gotcha on the subject asserted the opposite and has been rewritten. **(1)** New table `property_corner_source` (`migration_068`): `document_id` (FK → `document`, **UNIQUE**), `property_id` (FK → `property`), `created_at`, `created_by`. **The unique index IS the lock**: every creation path claims with `INSERT … ON CONFLICT (document_id) DO NOTHING … RETURNING`, and zero rows back means someone else already claimed it → 409. Atomic by construction — no `SELECT … FOR UPDATE`, no advisory lock, no provenance overloading, and no window between a check and a claim, because the insert *is* the check. Adrian's call over the cheaper "keep the lock on provenance": *"since provenance-as-lock is fragile: let's add that honest link; I don't like fragile!"* **(2) No backfill, and none is possible** — nothing in the DB recorded which document produced which existing Property, and matching by corner geometry would be a guess that permanently mislabels a document's origin. Stated in the migration header: documents imported before 068 stay **unlocked**, accepted deliberately. **(3)** The Process route's whole claim block (the `entity_metadata` row-lock transaction, the `=== "COORDINATE_FILE"` check, the claim UPDATE, the 409 and the catch-block provenance reset) is replaced by a cheap pre-check plus `claimCornerSource`. The claim happens immediately after `createProperty` — the Property must exist before anything can point at it, so a genuine race costs one insert-then-delete rather than a nullable link column that would make "claimed but pointing nowhere" representable. The compensating-action pattern is **kept**, not replaced by an outer transaction (`createProperty` runs its own internal transaction for the version-0 snapshot; threading an outer `tx` through the properties query layer was rejected in #23.03), and the document's provenance stamp moved to the very end as a **best-effort** write — per #21.07.Import, a metadata failure must never turn a successful entity create into an error response, and here it would have deleted a correctly-created Property. **(4)** Both wizard paths write the link. `CoordinatePropertyDialog` gained a `documentId` prop (it never had one — `entry` is a local file handle, not a Document) and now **claims before it PATCHes**: the link is the permission to write those corners, so recording it afterwards would let a file belonging to another Property overwrite this one's geometry first. It also gained a fifth pre-decided outcome — *this file already built a different Property* — checked before the user is asked anything, since a replace/keep decision that would be refused anyway is a question worth not asking. For the bulk import loop, `PropertyStepDialog`'s `ResolvedProperty` gained `cornerSourcePath`: the property step resolves the Property *before* the import runs, when the coordinate file is still a file handle with no `document` row to point at, so the claim genuinely cannot happen there — the loop takes it **the instant that file's Document is created**, before its pages, its property link and its tags. `cornerSourcePath` is keyed on whether corners were actually **written**, not on whether a file was picked: if the Property already had corners and the user chose *Păstrează*, the file was parsed and discarded, so it did not build this Property and must stay free to build another. **(5)** Provenance keeps being stamped where the code has parsed proof — it is accurate metadata now, and nothing reads it as a flag. **(6)** The Process panel names the Property it already produced, as a link to `/properties/{id}`, instead of a grey disabled button and a sentence telling the user to go and look — two overlapping polygons look like one until you count the rows, which is what made the original duplicate so hard to see. It asks a new read-only `GET /api/documents/[id]/corner-source`; the matching `POST` is the wizard's claim endpoint. A 409 now switches the panel to its *done* state rather than showing an error: the user's intent is satisfied, just not by this click. `principalObjectId` fell out of `ProcessPanel` and `DocumentDetailTabs` with the metadata fetch it existed for. **(7) Soft-delete cleanup.** Properties soft-delete, so the table's `ON DELETE CASCADE` never fires on the normal path and a stale link would block its source document forever. `softDeleteProperty` hard-deletes the link row — same shape as the CNP-uniqueness problem (a partial unique index cannot see the parent's `deleted_at`) but solved by cleaning up on delete rather than with a trigger: the link carries no history worth keeping, a trigger would hide a second source of truth in SQL, and with one delete path the thing a trigger buys you is worth nothing. This is also what makes re-pointing work — soft-delete the wrong Property, which frees the document, then re-run. **(8) The trade-off, put to Adrian and accepted:** `UNIQUE(document_id)` means one coordinate file can only ever be the origin of one Property, permanently — re-pointing a link is not an `UPDATE`. He was offered a direct re-point mechanism and declined it: the recovery route is to soft-delete the wrong Property, which frees the document, then re-run the correct path. So a future "we need to move a link" request is a change of mind, not an oversight — the constraint is deliberate. **(9)** Tests pin the contract rather than the functions: the client helper's idempotency rule (a claim losing to a link that already points at the *same* Property is `already-ours`, not a conflict — that one distinction is what makes claim-then-write retry-safe), the route's 409-names-the-winner behaviour, and two source-level invariants — that nothing anywhere compares `provenance` to `"COORDINATE_FILE"` to decide already-processed, and that `softDeleteProperty` still releases the claim. **(10)** Folded in: `coordinate-property-dialog.tsx` was left **half-converted** by #23.05.UX — one button used `buttonClass`, six below it were hand-written `bg-cta`/`border-wire` strings. The single-source test greps for `disabled:opacity-*` and none of them carried one, so it passed. Converted, which is the outcome that slice's "convert every button in every file the slice touches" rule was for. ✅

- Slice #23.07.Import — **A confirmed suggestion is not a silent inference, and a file name is a ranking signal.** Two things from Adrian's UAT pass, both about the import wizard's property step. **(1) Tarla/Parcela.** A Property created through the wizard arrived with both fields empty, while the same coordinate file processed from the Document detail page filled them — because `/api/documents/[id]/process` still calls `parseFolderName` and the wizard, since #23.00.Import, deliberately does not. `PropertyStepDialog`'s create-new branch now shows **Nr. tarla / sola** and **Nr. parcelă** inputs, pre-filled from the picked folder name by a new pure `cadastralSuggestionFromFolderName` (`src/lib/import/coordinate-file.ts`). ⚠️ **This does NOT contradict the standing "do not reintroduce `parseFolderName` into the import wizard" rule** — see the amended gotcha below. That rule forbids a value the system decides ALONE and writes without showing anyone; #23.00 retired the heuristic because the wizard silently stored tarla "3" for "3 Calea Victoriei". What ships here is *shown*: two labelled, editable inputs, described in Romanian as a suggestion, that the user confirms before the POST. **The bar for suggesting anything is the full `<tarla>-<parcela>` shape, not `parseFolderName`'s leading-digit test** — with no separator the parser hands back the entire name as the tarla, which is the same false positive wearing a different hat, and there is no parcela to go with it either way. "2024-Arhiva" **is** still suggested (tarla "2024" / parcela "Arhiva") and that is the design working, not failing: the residual guess is visible and one keystroke from cleared. Pinned by test so a future change to it is noticed. **`perToSlash` is applied**, because the Process route has always applied it before writing these columns — suggesting the raw "47per2" would have left two Properties whose cadastral identifiers differ only in encoding, which is the very asymmetry this slice removes, reintroduced one layer down. The values travel in the **same `POST /api/properties` body as the corners** so they land in version 0 (#23.00's rule: create-then-patch would open the Property's history with an edit nobody made); no schema change was needed — `propertyCreateSchema` derives from the Drizzle table and has always accepted them. The inputs render **unconditionally**: `lookup_property_type.show_tarla_parcela` governs the panel on the Property form, but this branch picks no property type, so there is no flag to consult. Adrian's call, made knowing a Property later typed with the flag off holds values its own detail form will not show. **(2) Coordinate-file name confidence.** Adrian's observation: "a Coordinate File not only has an extension TXT but also the file name starts with 'coord', for example: `coord 47per2-225per3per24-2716.txt`… check both, to gain more confidence that the system is reading the appropriate file and the creator of the file has not made a mistake." New pure `coordinateNameConfidence(name)` returns `"strong"` (shortlisted extension **and** the name folds to a `coord` prefix) / `"weak"` (extension only) / `"none"` (never a candidate). **The name is a ranking signal and a warning, never a filter** — `COORDINATE_FILE_EXTS` and `coordinateCandidates` are untouched, only the parse decides usability, and a correctly-formed export with an unconventional name still imports. Two behaviours follow. When two or more candidates parse with real corners, #23.00 required a manual choice (rightly: "most corners wins" would let a stray CSV take it silently); **exactly one `strong` candidate is now pre-selected**, because a naming convention is a statement the file's author made on purpose and is better evidence than a corner count — two or more `strong` is a tie the name cannot break, so the user still chooses, and every candidate stays visible and selectable either way. When exactly **one** candidate parses but its name is only `weak`, a quiet Romanian note (`role="status"`, small amber text, no banner) says so — Adrian's "the creator of the file made a mistake" case, surfaced rather than silently accepted, and it blocks nothing. The note is derived from `candidates` at render time, not copied into state, so there is no second copy to drift. **Implementation notes:** confidence folds case and Romanian diacritics through the existing `foldRomanian` (`src/lib/import/id-card.ts`, NFD — covers both the comma-below U+0219/U+021B and cedilla U+015F/U+0163 encodings of ș/ț) and then does a plain `startsWith` on the folded string. **No `\b` anywhere** — see the ASCII-only gotcha; a test named for it asserts `"Împrejmuire coord.txt"` is `weak`, which a `\b`-based implementation would get wrong rather than merely answer differently. The written-out Romanian form ("coordonate…") matches the same prefix, which is the same convention spelled out. **Also folded in:** the dialog's three free-text inputs (nickname, tarla, parcela) now share one `TEXT_INPUT_CLASS` constant instead of a thrice-copied Tailwind string. **New i18n keys** under `adminImport.wizard.propertyStep` in both locales (`tarlaLabel`, `parcelaLabel`, `cadastralHint`, `cadastralHintEmpty`, `coordinateNameNote`). **No DB, migration, schema, route or help-registry changes**, and `process/route.ts` / `addEntityTag` were not touched — their `parseFolderName` use remains the documented, out-of-scope gap. ✅

- Slice #23.08.Import — **One button on an ID-card row, because the second one cost more and returned less.** Adrian's question: an imported ID-card JPG offered both "Creează persoană" and "Interpretează cu AI" — why two? They did different jobs, but not *complementary* ones. `POST /api/documents/[id]/ai-interpret` builds its prompt from the document type's `template_fields`, and **CARTE_IDENTITATE has no template**, so on a card it asked for the four generic baseline fields and little else — while `extract-id-card` had already read the card number, the issuing authority and both validity dates. A second Anthropic call, billed in full, returning strictly less than the first one already had. Adrian's choice of the three options offered: fold it in. **(1) The mapping, as shipped** (pure `documentFieldsFromIdCard` in `src/lib/import/id-card.ts`): `idCardNumber`→`nrDocument`; `idValidFrom`→`dateDocument` (valid-from IS the issue date on a Romanian CI, which is what that column means for this type); `idValidUntil`→`dateValidUntil`; `idIssuingAuthority`→`subject` as `"Eliberată de {autoritate}"`; `lastName`+`firstName`→`title` as `"CI {Nume} {Prenume}"`. **(2) What is deliberately NOT mapped, and why it is the more important half.** `cnp`, `dateOfBirth`, `placeOfBirth`, `gender` and `idMrzRaw` describe the PERSON and are already written to `natural_person` by the same click; copying them onto the Document would create a second, freely-editable copy of a CNP that `migration_025`'s trigger makes **immutable** on the person — two sources of truth for the one field the schema goes out of its way to protect. `institutionId` is not a target either: it is an FK to `lookup_institution`, so resolving "SPCLEP Bragadiru" would mean auto-creating lookup rows from an AI reading, which is the exact trap the `KNOWN_TYPE_KEYS` gotcha records — the authority goes into free-text `subject` instead. No `customFields` key is invented: with no `template_fields` for the type, a key written without a template is invisible in the document form. Richer per-field capture on ID cards is a `template_fields` template — a **data** change, not code. **(3) Write-if-empty**, every target. In practice the two rules coincide (the bulk import created this Document seconds earlier and every target is null), but it makes a second click, or a card re-read against a document someone has since filled in, non-destructive for free: **nothing a human typed is ever overwritten by a machine reading**. **(4) One PATCH, restated as a rule.** Fields + the notes append + `aiInterpretedAt` travel in a single `PATCH /api/documents/[id]`, because `document` is versioned and two PATCHes mean two `document_version` rows for one click (the defect #23.02.Import recorded). When every target is already filled the mapping returns `{}` and only `aiInterpretedAt` is sent — that column is not in the snapshot, so the no-op backstop appends **no** version row rather than an empty one. A GET precedes the PATCH (reads append nothing) because both write-if-empty and the notes append need to know what is already there. **(5) The notes line is hardcoded Romanian, and that is not a two-track i18n violation.** The rule governs UI strings — text next-intl renders for whoever is looking. The moment this sentence is written into `document.notes` it stops being UI and becomes user DATA in a Romanian record; sourcing it from the active locale would let an `en-GB` session permanently stamp an English sentence into a Romanian document. It carries no timestamp (that is what `aiInterpretedAt` is for) and is guarded by a deliberately machine-shaped marker `[CI-AI]` so a re-run appends nothing — and it is only added **when a field was actually written**, since a note claiming data came from the card, on a document where none did, would be a lie in the one place nobody would check. **(6) Both branches write.** Create-new uses the CORRECTED form values (fixing a misread card number in the review form fixes `nrDocument` too, with no second set of inputs); confirm-existing uses the raw extraction. Whether the holder turned out to be new says nothing about the card's own fields. **(7) The write is LAST and cannot fail the action.** By then the Person exists and is linked to both the Property and the Document — the outcome the run exists to produce — so a failed PATCH is reported *on the row* (amber `⚠`, distinct from an errored row) via two new `ImportResult` fields, rather than being allowed to discard the work or to vanish. A failed *read* is likewise treated as a failed write, never as "every field is blank", which would invite an overwrite. **(8) `!isIdCard` added to the interpret gate** in `bulk-import-dialog.tsx` — the other half of the same change. **(9) `CARTE_IDENTITATE_ALT` removed from `ID_CARD_TYPE_KEYS`.** It had been kept as a "defensive match" and the justification does not survive inspection: the array is matched against Haiku's `suggestedTypeKey`, whitelisted against `KNOWN_TYPE_KEYS`, which lost the key in #23.01; and it cannot arrive from the DB either, because an unseeded key never reaches a row under its own name (`ensureDocType` misses `typeMap` and auto-creates a type from the free-text label, generating a *different* key). Confirmed against the live DB: 26 rows, not among them. The three real alternate wordings from `migration_021` are `AUTORIZATIE_ALT`, `CERTIFICAT_SARCINI_ALT`, `EXTRAS_CARTE_FUNCIARA_ALT` — **`CARTE_IDENTITATE_ALT` looked like a fourth member of that family and never was one, and that resemblance is exactly why it survived**; the comment now names the real three so the next reader can tell a member from a lookalike. Kept as a one-element array, since a genuine alternate wording is then a one-line addition. **(10) Folded in**, per #23.05.UX's "convert every button in every file the slice touches": the ID-card dialog's fatal-error dismiss button and the three import-row action buttons were still hand-written (`bg-cta …` / `border-cta/40 bg-cta-pale …`) — invisible to `button-styles-single-source.test.ts` because none carried a `disabled:opacity-*`, the same leftover class #23.06.Import found in `coordinate-property-dialog.tsx`. `ghost`/`xs` and `primary`/`lg` reproduce their existing geometry exactly, and add the focus ring and dark-mode rules they never had. **No DB, migration, schema or route changes**; new i18n keys in both locales. ✅

Each slice typically lands as multiple small commits, each individually green.

## Versioning pattern & pitfalls (Property + Person + Document all done)

Property versioning shipped in Slice #18.02; Person versioning (both subtypes) shipped in Slice #18.05; Document versioning shipped in Slice #18.06. All three core entities are now versioned. This section is the canonical reference for the pattern — read it before adding versioning to any future entity or extending the existing ones.

**Document-specific notes (Slice #18.06) — the simplest application:**
- **Single table, flat snapshot.** No subtypes, no corners, no satellites — `DocumentSnapshot` is just the document's 21 own fields, all `string | null`, so the snapshot IS the flat field map (`computeFieldHighlights` diffs it directly, no flattening step). `suprafata` is the numeric column read as a string; cast `::text` in the migration backfill to match.
- **Scope = form fields only.** The uploaded `document_page` files and the M:M associations (persons / properties / documents) are deliberately OUT of the versioned scope — they have their own immediate-save lifecycles, like Property's associations and Person's associations.
- **`updateDocument` was wrapped in a transaction** for this slice (it previously did a bare update) so the version-append insert is atomic with the field patch.
- **Reuses the shared primitives** from `src/lib/versioning/field-diff.ts` (the entity-neutral diff core — relocated here from `src/lib/persons/version-diff.ts` once Document became the third consumer) and the shared `VersionNavControls` (`src/components/version-nav-controls.tsx`). i18n keys live under the `document` namespace as `version.*` + a `makeCurrent.*` confirm block.
- **Type-specific field visibility:** the snapshot always captures all 21 fields regardless of which are visible for the current document type; highlight frames render on whatever fields are visible for the viewed version's type, while the label colour reflects ALL field changes (even ones hidden under the current type). `documentTypeId` is itself a versioned field, so changing the type reads as a red modification.

**Person-specific notes (Slice #18.05), as a template for multi-subtype entities:**
- **Two subtypes, one shared table.** Natural and judicial persons both FK `person.id`, so a single `person_version` table serves both; the snapshot JSON shape simply differs by `person.type`. `listPersonVersions` (in `src/lib/persons/queries.ts`) is type-agnostic; each route/form casts to its subtype snapshot. The natural snapshot build + equality live in `persons/queries.ts`; the judicial ones in `judicial-persons/queries.ts`.
- **No corners** — drop the entire corner-diff clause. The diff is purely field-level (own fields + `person.notes` + the owned address blocks). The shared pure primitives (`fieldFrame`, `diffFieldMap`, `fieldMapsEqual`, `labelColorFromHighlights`) live in `src/lib/versioning/field-diff.ts`; each subtype's `form-schema.ts` supplies its field-key list and builds `computeFieldHighlights` / `versionLabelColor` / `snapshotToFormValues` / `formValuesEqual` on top.
- **Address-block highlights** reuse the shared `AddressBlock` component, which gained an optional `highlights` prop (per-subfield green/red ring). The judicial form inlines its own `AddressFields` (single Office Address card) and got the same `highlights` wiring.
- **Booleans in the snapshot** (judicial `correspondenceSameAsHq`) are diffed by stringifying to `"true"`/`"false"` in the field map (so a toggle reads as a red modification) while `formValuesEqual` compares them as booleans.
- **The version nav sits on the entity-name header line.** Each detail-tabs component renders a centered `pointer-events-none` slot in its `<header>`; the form portals the shared `VersionNavControls` into it. i18n keys live under each namespace as `version.*` (label/prev/next/makeCurrent/makeCurrentHint) + a `makeCurrent.*` confirm block.
- **Judicial update writes the version inside the tx** from a tx-consistent refetch — `getJudicialPersonById` (the function it returns) reads via the global `db` connection and would not see the tx's uncommitted writes.

Property versioning shipped in Slice #18.02. The Property recipe below is the canonical reference; the Person/Document notes above record where each diverged.

**Person-specific notes (Slice #18.05), as a template for Document:**
- **Two subtypes, one shared table.** Natural and judicial persons both FK `person.id`, so a single `person_version` table serves both; the snapshot JSON shape simply differs by `person.type`. `listPersonVersions` (in `src/lib/persons/queries.ts`) is type-agnostic; each route/form casts to its subtype snapshot. The natural snapshot build + equality live in `persons/queries.ts`; the judicial ones in `judicial-persons/queries.ts`.
- **No corners** — drop the entire corner-diff clause. The diff is purely field-level (own fields + `person.notes` + the owned address blocks). The shared pure primitives (`fieldFrame`, `diffFieldMap`, `fieldMapsEqual`, `labelColorFromHighlights`) live in `src/lib/versioning/field-diff.ts`; each subtype's `form-schema.ts` supplies its field-key list and builds `computeFieldHighlights` / `versionLabelColor` / `snapshotToFormValues` / `formValuesEqual` on top.
- **Address-block highlights** reuse the shared `AddressBlock` component, which gained an optional `highlights` prop (per-subfield green/red ring). The judicial form inlines its own `AddressFields` (single Office Address card) and got the same `highlights` wiring.
- **Booleans in the snapshot** (judicial `correspondenceSameAsHq`) are diffed by stringifying to `"true"`/`"false"` in the field map (so a toggle reads as a red modification) while `formValuesEqual` compares them as booleans.
- **The version nav sits on the person-name header line.** Each detail-tabs component (`natural-persons` / `judicial-persons` `_components/person-detail-tabs.tsx`) renders a centered `pointer-events-none` slot in its `<header>`; the form portals the shared `VersionNavControls` (`src/components/version-nav-controls.tsx`) into it. i18n keys live under each person namespace as `version.*` (label/prev/next/makeCurrent/makeCurrentHint) + a `makeCurrent.*` confirm block — NOT under a `corners` namespace.
- **Judicial update writes the version inside the tx** from a tx-consistent refetch — `getJudicialPersonById` (the function it returns) reads via the global `db` connection and would not see the tx's uncommitted writes.

Property versioning shipped in Slice #18.02. The Property recipe below is the canonical reference; the Person notes above record where Person diverged.

### Design — full snapshots, not deltas

- One `<entity>_version` table: `id` uuid PK, `<entity>_id` uuid FK (ON DELETE CASCADE), `version_number` int (0-based), `snapshot jsonb NOT NULL`, `created_at`, **unique `(<entity>_id, version_number)`**.
- Each save stores a COMPLETE snapshot (all versioned fields) as JSONB. Reconstructing "version N" is a direct lookup — no delta replay. (Adrian first suggested v0 + deltas; we changed to full snapshots for robustness. The "delta" still exists — it's just computed on the fly by diffing two snapshots at display time, and nothing derived is persisted. This honours the project's "storage is cheap; correctness/simplicity over efficiency; full over delta" rule.)
- **Versioned scope = the entity's own form fields** (plus satellite blocks it owns: Property's address + corners; Person's address(es); etc.). **NOT** the M:M associations (persons/documents/properties) — those live on other tabs and are out of scope.

### Rules (identical across entities; drop the corner clause where there are no corners)

- Version 0 = state at creation (written inside `createX`'s transaction).
- Each saved edit appends `max(version_number)+1` — but **skip the insert if the new snapshot equals the latest stored one** (no-op backstop), compared **field-by-field, NOT `JSON.stringify`** (Postgres jsonb does not preserve object key order).
- **Label colour**: v0 always green; otherwise red if any field was modified or deleted, OR (Property) corners changed in ANY way including a pure addition; green only for additions-with-no-corner-change.
- **Highlight frames** (shown only on a read-only historical version ≥ 1, diffing N vs N-1): green = added field, red = modified/deleted field. Property corners are per-row and **always red** for added/changed corners; a **removed corner renders as an empty, full-height red row at its former position** (so the table doesn't shrink). Corner-diff strategy: when the corner **count is unchanged** (in-place edit or reorder) diff **positionally** (`same`/`changed`) so the row count never changes; only when the count **differs** (genuine add/remove) fall back to an **LCS diff** that places the removed marker at the right spot. (Do NOT diff corners purely by LCS — a reorder/edit then renders as remove+add and grows the table by a row.)
- **Only the latest version is editable**; every earlier version is strictly read-only. Edit-save **stays on the entity page** (create/delete/cancel still navigate away).
- **"Make this version current"** — a button on the nav line (same level as ◀ / label / ▶), enabled **only while viewing a past version** (disabled on the latest). After an OK/Cancel confirmation describing the change, it restores the viewed snapshot as a **new version** (number = latest+1) by re-saving it through the normal save path (`doSave(form.getValues())` with the viewed corners), then follows the new latest — same stay-on-page baseline reset as an edit-save. No new endpoint needed.

### Layers to touch (mirror Slice #18.02's file set)

1. `migration_NNN_<entity>_versions.sql` (new) — create table + **backfill version 0 for every existing row** (idempotent: skip rows that already have a v0). The backfill's `jsonb_build_object` shape MUST match the JS snapshot shape exactly — **cast numerics to `::text`** so they match drizzle's string reads. Apply locally via `docker cp` + `psql -f` (NOT `npm run db:migrate`), to Supabase via SQL Editor, and add the table to `supabase_schema_full.sql`.
2. `src/db/schema/index.ts` — add the version table (`jsonb("snapshot")` left untyped to avoid a circular import; cast to the snapshot type in the query layer).
3. `src/lib/<entity>/validation.ts` — shared `EntitySnapshot` (+ sub-shapes) types. Pure types, safe to `import type` from client modules.
4. `src/lib/<entity>/queries.ts` — `snapshotFromFull`, `snapshotsEqual` (field-by-field), write v0 in `createX`, append-with-dedup in `updateX`, `listEntityVersions(id)` (oldest-first).
5. `src/app/api/<entity>/[id]/versions/route.ts` (new) — read-only `GET` → `{ items }`.
6. form-schema (pure, **unit-tested**): `snapshotToFormValues` / `snapshotToCorners`, `computeFieldHighlights(prev,curr)`, `computeCornerDiff(prev,curr)` (positional same/changed when count equal; LCS same/added/removed when count differs), `versionLabelColor(prev,curr)`, `cornersChanged`, `formValuesEqual`, plus types `HighlightColor` / `FieldHighlights` / `CornerDiffEntry` / `VersionNav`.
7. form component + the child that hosts the nav line; i18n keys `versionLabel` ("version {n}"), `prevVersion`, `nextVersion` (+ `removedCorner` where there are corners); tests.

Nav-line spacing (Adrian's spec, base gap `g`): `[+ Add] —g— [Show Big Map] —2g(ml-8)— [◀] —g(ml-4)— [version N] —g(ml-4)— [▶]`; `first:ml-0` zeroes the leading margin when earlier controls are hidden in read-only views.

### Pitfalls hit during Property versioning — avoid these next time

1. **Apply the migration before testing.** The save writes a version row in the *same transaction* as the field update; if the table doesn't exist the whole save rolls back (looks like "save failed / 500").
2. **Expired Supabase session = silent fake-success.** When the session expires, the auth middleware redirects the `PATCH` to `/sign-in` and `fetch` follows it as a **200** (sign-in HTML) — so the save *looks* successful (it redirects) but nothing persisted ("redirects but change is gone"), with a **401** visible on a token request in the console. Immediate fix: re-login. Permanent guard (`if (res.redirected) throw new Error(t("saveErrorSession"))`) is in the Property, Natural-Person, Judicial-Person, and Document `doSave` paths, each with a `saveErrorSession` i18n key in its namespace. Add it to any new mutating form too.
3. **React-Query cache hides the new version.** A too-high `staleTime` on the versions query serves the stale list (symptom: "stuck at version 0"). Use `staleTime: 0` + `refetchOnWindowFocus: false`, and **invalidate `["<entity>-versions"]` in `doSave` on success**.
4. **A disabled `<fieldset>` disables EVERY descendant control** — including the ◀/▶ nav buttons — and a child cannot be re-enabled per-button. Scope the read-only `<fieldset disabled={...}>` to wrap **only the editable input sections**; render the version-nav line **outside** it (the corners/nav child enforces its own read-only via a `readOnly` prop). Otherwise the arrows go dead on every read-only historical version (you can step back once from the latest, then get stuck).
5. **`editDirty` must compare to a baseline held in state, not RHF's `isDirty`.** Version navigation calls `form.reset(...)`, which clears RHF's `isDirty`. Compare watched values (+ corners) to a baseline initialised from `initialValues`/`initialCorners`; update that baseline after an edit-save so the form goes clean in place.
6. **Stay-on-page after an edit-save.** On success: reset the baseline to the just-saved state (Save disables), set `viewingVersion = null` (follow the new latest), and `router.refresh()` for server-rendered bits (e.g. the page title). The versions query invalidation makes the nav jump to the new version.
7. **Lock the nav while the latest is dirty.** Disable ◀/▶ when on the latest with unsaved edits, so a dirty draft is never stranded on a read-only historical view (where the page-leave guard wouldn't fire) — returning to the latest always restores the clean baseline.
8. **Zod 4 `z.string().uuid()` is strict** (it validates the version/variant nibbles). Test fixtures must use real-shaped v4 UUIDs (e.g. `...-4xxx-8xxx-...`); production ids are `gen_random_uuid` (v4) so live data is fine.
9. **Sandbox toolchain limits — see the authoritative gotcha below, not this line.** This pitfall used to say `tsc`/`jest`/`tsx` all fail in the sandbox. That is wrong about `tsc`, which is pure JavaScript and unaffected by the Windows-only native binaries. The current, correct statement lives in "The sandbox CAN run `tsc` …" under **Gotchas we've learned**; read it there, including the case where even `tsc` is out of reach. **Adrian still runs `npm run e2e` (dev server running separately) then `npm run lint` + `npx jest`** on his machine before committing — that has not changed.

## Document type onboarding — reference (Contract de Vânzare was the template)

Cross-cutting knowledge for every future document-type slice (Slice #21.03.Import / #21.04.Import onward) — read this once per new type rather than re-deriving it. Each new document-type slice's own description only needs a short checklist (field list for that type, groups, party roles if any); come back here for the "why" behind any checklist item.

### Field template design (`template_fields` JSONB on `lookup_document_type`)

A document type's entire custom-fields panel is **data on the DB row**, never a hardcoded per-type component. Adding a new type's template is one `templateFields` array — no code change, no deploy. Don't special-case a document type in `document-form.tsx`.

Per field:

- **`key`** — camelCase, becomes the JSON key in `document.custom_fields` *and* the AI-extraction field key. Treat it as permanent once real data exists under it — renaming means a migration to move data, not just a label edit.
- **`labelRo` / `labelEn`** — both required; the form always has one available regardless of locale.
- **`type`** — `text`, `textarea`, `date`, or `number`. Use `textarea` for anything that reads as a paragraph (boundary/neighbor descriptions, clauses, notes-like content) — it gets auto-grow and full-width for free. Use `date`/`number` where the value really is one, not `text` — it drives both input affordances and the AI-extraction format hint (`templateFieldFormatHint`).
- **`order`** — controls both display order within its group and extraction order in the AI prompt.
- **`aiHint`** — the single highest-leverage field for extraction accuracy. A concrete example beats an abstract description: `"e.g. '2.000,00 RON'"` extracts far more reliably than `"the sale price"`. Write hints as if training a new clerk who's never seen this document type.
- **`groupRo` / `groupEn`** — optional but should almost always be set. Fields sharing a group render together under their own titled panel; ungrouped fields fall into one panel titled with the generic "type-specific fields" label. A flat, ungrouped list of more than ~6 fields is hard to scan — group by real-world category (Contract de Vânzare's split was Financial / Land-cadastral / Certificates & references / Fees — reuse that pattern's *shape*, not necessarily those exact categories).

### Writing the template (mechanics)

- One `PUT /api/admin/value-lists/document-types/{id}` with the full `{ name, sortOrder, templateFields: [...] }` body writes the whole template at once (full-replace, not merge).
- The admin UI's own rename form only ever sends `{ name, sortOrder }` — a plain rename in the UI never clobbers an existing template. Setting/changing `templateFields` is always a deliberate, explicit call.
- `parseTemplateFields` never throws on malformed data — a bad row degrades gracefully (drops the bad entries) rather than breaking the form or the AI prompt. Still worth eyeballing the PUT response to confirm field count and a sample group came through as expected before moving on.

### Party / person extraction (only relevant if the type names people or organizations)

- Party roles (Vânzător, Cumpărător, Notar, Mandatar, etc.) are configured per document type in **Reference Data → Document Persons**, fully data-driven — the extraction code has no hardcoded notion of "seller" or "buyer". **Configure roles before testing AI Interpret** — if none are configured for the type, the prompt omits the parties section entirely and the response comes back with `partyRolesConfigured: false`, not an error, so a missing-roles config gap is easy to miss if you don't know to check for it.
- Matching against existing Persons is **exact-match first**: CNP for natural persons, CUI for judicial persons. Only when there's no exact match does it fall back to fuzzy name search — and those fuzzy hits are always labelled "unconfirmed", never auto-linked.
- Nothing is ever created or linked automatically. The confirm-or-create dialog is a one-at-a-time stepper requiring an explicit decision per party (confirm exact match / pick a fuzzy match / create new / skip). Keep this pattern for every new type with parties — it's the deliberate safety net against silently merging two different real people.
- `domiciliu` (address) comes back as unstructured free text from the model — stored as a single street-line, not decomposed into street/city/county. Don't try to make the AI parse structured addresses; it isn't reliable enough to be worth it yet.

### UI rendering (already built into `document-form.tsx` — nothing to redo per type)

- Every input/textarea in the document form has `spellCheck={false}` — Romanian text triggers false positives under the browser's default English spellchecker. Already global to the form; no per-type action needed.
- Textareas auto-grow to fit content via a `useEffect` keyed on a `watchValue` prop (sourced from `form.watch()`), **not** a native `onInput` listener — RHF's `setValue()` (used by AI Interpret to fill fields programmatically) doesn't dispatch a real DOM input event, so only a state-driven trigger catches both typing and AI-filled content.
- All `type: "textarea"` template fields automatically render at full section width (`fullWidth` prop → spans both grid columns) rather than a half-width cell. This is a blanket rule for the field *type*, not a per-field opt-in — nothing to configure per document type here.
- Grouped sections gracefully degrade: a type with no groups defined still renders correctly under one generic-labelled panel. Don't feel obligated to group a type that genuinely only has 2–3 flat fields.

### Testing workflow

- Test AI Interpret against a **real scanned sample** of the new type wherever possible — synthetic/hypothetical text misses real layout quirks (e.g. the notarial authentication block landing on the *last* page rather than the first, which is exactly what broke page-1-only extraction for Contract de Vânzare).
- The server console log (dev terminal, or the API response body directly when driving via browser automation) prints exactly what was extracted into generic fields, template fields, low-confidence fields, and `unmappedRaw` — read it, don't just eyeball the form.
- Anything that keeps showing up in `unmappedRaw` across multiple real samples of the same type is a signal to add it as a proper template field rather than leaving it to fall into Enhanced Notes indefinitely.
- **Coordinate live-DB testing.** When both Claude and Adrian are testing against the same dev server/database at the same time, records can appear to vanish or change out from under whoever's mid-flow. Whoever is about to drive the browser against the live dev app says so first and waits for a clear "go ahead" or "I'm done" from the other.

### Verification & delivery (sandbox-specific, not domain-specific)

- Sandbox verification: prefer a full-project `tsc --noEmit` where the sandbox can reach the toolchain, and fall back to per-file parser diagnostics only where it cannot. See "The sandbox CAN run `tsc` …" under **Gotchas we've learned** for which applies when — the blanket "the sandbox can't run the toolchain" claim that used to sit here was wrong about `tsc`. Neither is a substitute for Adrian's real `npm run e2e` → `npm run lint` → `npx jest`.
- Every delivered file still needs Adrian to run `npm run e2e`, then lint + Jest, locally before it's considered done — that hasn't changed and won't change per document type.

## Collaboration rules

- **Never commit or push without explicit confirmation.** Same for any irreversible action.
- **Conventional commits** — `feat:`, `fix:`, `chore:`, `ci:`, `docs(scope):`, `test:`, etc.
- **Always provide commit statements as ready-to-run PowerShell `git` commands**, not just the commit message text. Each commit should be a full `git add <files>` followed by `git commit -m "message"` that Adrian can paste directly into his terminal. **Never join them with `&&`** — Adrian's PowerShell is Windows PowerShell 5.1, which does not support `&&`/`||` as statement separators (`The token '&&' is not a valid statement separator in this version.`). Put each command on its own line instead (newline-separated is enough; PowerShell runs them sequentially regardless of exit code, which is fine for `git add` + `git commit`). If a single-line chain is genuinely needed, use `;` instead of `&&` — but separate lines are preferred for readability.
- **Always provide complete, ready-to-run PowerShell commands** for every step — including env var assignments, seed runs, migrations, etc. Never give a connection string or value in isolation; always show the full assignment (`$env:VAR = "value"`) as a separate line before the command that uses it. PowerShell cannot run a bare URL or string as a command.
- **Always check `git status` before making changes**, and never modify files outside `C:\dev\ga40prj`.
- **Adrian runs git in PowerShell on Windows.** Claude prepares file content; Adrian commits and pushes. This avoids Windows-mount permission issues with `.git/index.lock` from the Linux sandbox.
- **Trust HEAD as the source of truth.** The Linux sandbox can show stale or phantom file states (deleted files appearing as untracked, modified files showing clean, etc.). When in doubt, ask Adrian to `git status` on his side.
- **Secrets stay out of chat.** `.env` is gitignored; values live in `C:\dev.docs\ga40prj\` (Adrian's reference docs, not in the repo). Never echo passwords or API keys back into the conversation.

## Repo conventions

- **Line endings** — LF everywhere, enforced by `.gitattributes`.
- **Env vars** — `.env.example` is the source of truth for keys; `.env` holds values locally and is gitignored.
- **Docker** — compose lives in `docker/postgres/`. PostGIS extensions are created on first boot via `init/01-extensions.sql`. Secrets come in via `--env-file .env`.
- **Tests** — colocated under `src/__tests__/` for now; will move to `*.test.ts(x)` next to source files as the codebase grows.
- **Imports** — prefer absolute paths from `@/` (configured in `tsconfig.json`) over deep relative paths.
- **Buttons — always `buttonClass()`; never hand-write `disabled:opacity-*` again.** Every `<button>` takes its classes from `buttonClass({ variant, size })` in `src/lib/ui/button-styles.ts` (variants `primary` / `secondary` / `danger` / `ghost` / `bare` / `bare-danger`; sizes `xs` / `sm` / `md` / `lg`). Do not pass a `disabled` boolean — the helper emits both states and CSS picks via `:enabled`/`:disabled`, so the styling can never disagree with the `disabled={...}` prop. Layout-only extras (`ml-4`, `w-full`, `flex-1`, `gap-2`) go in `className`; anything that CONFLICTS with what the helper emits (`text-xs`, `px-4`, `rounded-full`) must come from `size` / `pill` instead — see the Tailwind stylesheet-order gotcha below. `src/__tests__/button-styles-single-source.test.ts` fails the build on a hand-written `disabled:opacity-*`. The helper is for form controls only: `:enabled`/`:disabled` never match an `<a>` or a next/link `<Link>`, so a link styled with it would render with no hover state at all.

## Gotchas we've learned

- **Two-track i18n (intentional architectural exception).** Every UI string goes through next-intl (`messages/en-GB.json` + `messages/ro-RO.json`). The one deliberate exception is **runtime-editable content** — text that Adrian edits live in the Admin UI without a code deploy. Today that is `help_content` (screen-level Background + How-To copy) and `help_hint` (inline micro-hints). Both tables store bilingual text as explicit DB columns (`*_en` / `*_ro`) rather than as next-intl keys, because the messages JSON files are baked into the build and cannot be changed at runtime. **The rule:** if a string is authored by a developer and changes only via a code commit → next-intl. If a string is authored by Adrian at runtime via an Admin screen → DB columns (`_en`/`_ro` suffix pattern, matching `help_content`/`help_hint`). Never put runtime-editable strings into messages/*.json (they would require a redeploy to take effect) and never put dev-authored static UI copy into the DB (that defeats next-intl type-safety and build-time checks).

- **Next.js 16 ≠ training data.** App Router has breaking changes; the old `i18n` config in `next.config.js` is gone (Pages-Router-only). Read `node_modules/next/dist/docs/` before writing routing or middleware code.
- **Dev-server route table can go stale after a long Fast Refresh session — symptom looks exactly like a data bug.** After many hours of hot-reloading (especially after adding several new API route files, as in a slice touching lots of `route.ts` files), `npm run dev` can start 404ing dynamic API routes — e.g. `GET /api/documents/[id]/pages/[pageId]/view` — even when the DB row and the underlying file both genuinely exist and are correct. Hit in Slice #21.10.help.rollout follow-up: verified the `document_page` row and its file on disk were both present and correct, yet every document's page-view request still 404'd — restarting `npm run dev` (Ctrl+C, then `npm run dev` again) fixed it instantly with zero code changes. This is dev-mode-only (Fast Refresh/HMR route-table drift); it cannot happen on Vercel or Ciprian's UAT box, which compile once and serve that build with no live route table to drift. **When a route 404s for a reason the code doesn't explain, restart the dev server before debugging further** — especially if several new route files were added recently in the same session.
- **Tailwind v4 has new syntax.** `@import "tailwindcss";` plus `@theme inline { ... }` instead of `tailwind.config.js`-driven theme keys. Don't reach for v3 patterns.
- **Appending a conflicting Tailwind utility does NOT override the earlier one — stylesheet order decides, not class-attribute order.** `"px-4 py-2 text-sm" + " text-xs"` does not reliably shrink the text: both `text-sm` and `text-xs` are real rules in the generated stylesheet, they have identical specificity, and the winner is whichever Tailwind emitted LAST — which is a function of Tailwind's own internal ordering, not of where you wrote them. The symptom is a class that "sometimes works", or works until an unrelated file starts using the other utility and shifts the emission order. This is why `buttonClass()` takes `size` and `pill` as OPTIONS rather than letting callers append `text-xs` or `rounded-full`, and why its `className` parameter is documented as layout-only (`ml-4`, `w-full`, `flex-1`, `gap-2` — utilities the helper never emits, so nothing can conflict). The same trap applies to any shared class-string helper: whatever the helper decides, the caller cannot un-decide by appending. Where a caller genuinely needs a different value, add it to the helper's option set. (Hit while designing the helper in Slice #23.05.UX; the four button sizes exist precisely so no call site ever needs to fight the helper for a padding or a text size.)
- **A colour set on a child element beats the parent button's hover colour — an inverting hover must force its descendants.** `buttonClass`'s `secondary` variant flips `text-cta` → `text-white` on hover, but a label rendered as `<span className="text-fade">…</span>` keeps its own colour: inheritance only applies where the child sets nothing. The button turned dark slate (#334155) while its label stayed #595F6A — a **1.63:1** contrast, unreadable — and only on hover, so it survived every static review. Found by Adrian on the "Câmpuri afișate" column pickers; seven buttons were affected across the three list views and `add-property-dialog.tsx`, all of them `secondary`. Fixed once in the helper with Tailwind v4's `**:` descendant variant (`enabled:hover:**:text-white`) rather than at the call sites, so the button owns its label colour outright and a future call site cannot reintroduce it by wrapping a label in a coloured span. The override is hover-only — at rest a muted child label is legitimate (`text-fade` on `bg-cta-pale` is 5.8:1) and the design uses it deliberately. **The general rule: any variant that CHANGES text colour on a state needs `**:` on that state; a variant whose text colour is constant does not.** That is why only `secondary` carries it — `primary` and `danger` are white throughout, `ghost` and the `bare` pair never invert.
- **A disabled `<button>` still matches `:hover`.** `disabled` stops clicks and focus, not hover styling — so `hover:bg-cta-d` repaints an inert button when the mouse crosses it, undoing whatever the disabled state was meant to communicate. Write `enabled:hover:` on any button whose hover changes colour. It also removes a variant-ordering question entirely: `:enabled:hover` and `:disabled` are mutually exclusive selectors, so neither can win over the other regardless of what order Tailwind emits them in. Every hover rule in `src/lib/ui/button-styles.ts` is written this way and a test asserts no bare `hover:` slips back in.
- **Write tool truncation on `$`.** When writing files containing shell-style `${VAR}` references (e.g. docker-compose), use bash heredoc with a single-quoted delimiter (`<< 'EOF'`) instead of the Write tool.
- **The sandbox CAN run `tsc` on the whole project — it is `jest` and `next` that cannot.** CLAUDE.md previously lumped all three together as unrunnable. In fact `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` completes in well under a minute and exits 0 with zero diagnostics: `tsc` is pure JavaScript, so the Windows-only native binaries are irrelevant to it. `jest` genuinely cannot run — `next/jest` needs the SWC transformer, and the Linux `@next/swc-*` binaries are not installed; Next.js then tries to download a WASM fallback from `registry.npmjs.org`, which the sandbox has no route to (`EAI_AGAIN`). ⚠️ **But `tsc` needs the repo on a fast filesystem, and that is not always the case.** When the session reaches the repo over the remote-device bridge (Cowork's cloud mode) rather than having it locally, a full-project run is **I/O-bound on the mount, not on the CPU**: measured at 42 s wall for 4 s of user time, reading `node_modules` at roughly 1.7 MB/s, and it does not finish inside the 45 s per-command limit. Backgrounding does not help — processes are killed when the command returns — and rebuilding `node_modules` in the cloud container fails too, because the sandbox's npm registry allowlist blocks several of this project's packages (`zod`, `@hookform/resolvers`). Hit in Slice #23.06.Import. **The fallback that does work in that situation:** run TypeScript's parser over each changed file (`ts.createSourceFile(..., /* setParentNodes */ true)` and read `sourceFile.parseDiagnostics`) — it costs milliseconds per file, needs no module resolution, and catches the whole structural class, including the JSX one that is easy to write and impossible to spot by eye (`{/* comment */}` is legal between JSX children and a syntax error inside an opening tag's attribute list). It is NOT a type check: say so plainly when handing over, and make `npx tsc --noEmit` the first thing Adrian runs, ahead of e2e.

**So: always run a full-project `tsc --noEmit` before handing work over** — it is a real type check across every file, not a filtered-for-noise approximation, and it catches far more than the brace-balance heuristic. Adrian still runs `npm run e2e` (dev server running separately), then `npm run lint` + `npx jest` (ESLint rules, actual test execution, and real-browser behaviour all remain unverifiable here). A pure module can also be spot-checked behaviourally by compiling it with `tsc --module commonjs --outDir /tmp` and driving it from a small Node harness — that is how `discover-log.ts` was validated in Slice #21.10.Import without Jest.
- **`npx tsc --noEmit` type-checks Next's GENERATED route types too — and `next dev` never prunes them, so a deleted route errors for weeks afterwards.** `tsconfig.json`'s `include` carries `.next/types/**/*.ts` and `.next/dev/types/**/*.ts` alongside `**/*.ts`, so `tsc` compiles `.next/types/validator.ts` — a Next-generated file holding one `typeof import("../../src/app/…/route.js")` per API route. **`next dev` adds entries there but does not remove them when a route file is deleted.** The result is `error TS2307: Cannot find module '../../src/app/api/…/route.js'` naming a file that is *supposed* to be gone, pointing at a path under `src/` that no longer exists, in a file nobody wrote and git does not track (`.next/` is gitignored). Hit in Slice #23.06.Import: `npx tsc --noEmit` reported exactly one error, for `api/admin/import/extract-document/route.js` — a route correctly deleted by **Slice #23.04.Import**, eight commits and two slices earlier. The `.next/types/validator.ts` holding the reference was dated a week before, and 145 of its 146 route references were fine. **The tell:** the error is in `.next/`, not in `src/`, and the module it cannot find is one you meant to delete. **Fix:** `Remove-Item -Recurse -Force .next` (safe — generated, gitignored, rebuilt by `dev`/`build`), then re-run. Note that with `.next` absent the two `.next/types` globs match nothing, so `tsc` then checks your source only; to get the route-validator coverage back, run `npm run build` once — a clean build enumerates the routes that actually exist — and then `tsc` again. **The general lesson: a `tsc` error whose path starts with `.next/` is a statement about the build cache, not about the code, and the code it accuses may be innocent precisely because it was deleted correctly.**

- **Stop `npm run dev` before `npx tsc --noEmit`, for the same reason as jest.** `next dev` rewrites `.next/` continuously, and `tsc` reads `.next/types/**` as part of its input — so a run started while dev is live is compiling a tree that is being edited underneath it, on top of contending for the same disk and the couple of GB dev is holding. The symptom Adrian saw was not an error but **no output at all**; killing the dev server and re-running produced an answer immediately. Treat the whole verification sequence as: dev running only for `npm run e2e`, then stop it for `npm run lint`, `npx tsc --noEmit` and `npx jest`.

- **`npx jest` while `npm run dev` is still running → workers OOM, and it looks like a test failure.** The verification order (`npm run e2e` → `npm run lint` → `npx jest`) *requires* a dev server for the e2e suite, so it is still holding a couple of GB when Jest starts a few seconds later in another terminal. Jest forks `cores-1` workers; the ones that lose the race die at spawn with `Out of memory: HashMap::Initialize` or `Zone Allocation failed` at a 40–80 MB heap — a **system** allocation failure, not a heap one — and are reported as `Test suite failed to run — Jest worker ran out of memory`. **The tell:** `Tests: N passed, N total` with **zero failed assertions**, and the casualties are the smallest, cheapest suites (pure-module tests importing one file) rather than the heavy ones — `help-coverage.test.ts`, which reads every `.tsx` in `src/`, passes right alongside them. Nothing about the code under test is implicated. **Fix:** stop the dev server (Ctrl+C) before `npx jest`, or run `npx jest --maxWorkers=2`. Confirm it by re-running just the failed suites — they pass in about a second. Hit in Slice #23.04.Import, where it cost a round trip diagnosing a deletion-only slice for a memory regression it could not have caused (the same full suite then ran in 4.4 s instead of 34 s with the dev server stopped, on identical code).
- **Sandbox file drift from Windows.** Occasionally the Linux sandbox shows files as deleted/added when Adrian's Windows side is clean. Don't react to it — verify on his side. `tsc --noEmit` in the sandbox also shows phantom JSX parse errors on perfectly valid files that are already committed; these are sandbox artefacts, not real errors. The sandbox cannot run `jest`/`tsx` (Windows-only esbuild/SWC binaries in `node_modules`); whether it can run `tsc` depends on how the repo is mounted — see the `tsc` gotcha below. Always verify with `git diff` on Adrian's side, and have Adrian run `npm run lint` + `npx jest`.
- **Outputs-scratchpad sync lag (Windows-tool writes → bash reads), one-directional.** When editing a large file in the temporary outputs scratchpad (e.g. a docx `document.xml` during an unpack → edit → pack cycle), edits made via the Read/Edit/Write tools can take a long time (confirmed 40+ minutes, not resolved by `sleep`/`sync` retries) to become visible to bash `cat`/`wc` on the same nominal path — bash sees a stale, truncated snapshot. The reverse direction is reliable: anything written via bash is visible to the Windows-side tools immediately. Symptom: `pack.py` (which runs in bash) reports a premature/truncated XML error even though the Windows-side `Read` tool shows the file as complete and well-formed — this is the scratchpad lag, not a real XML mistake. Workaround: pull the correct content via `Read` (in chunks if the file is long) and write it into the bash-mounted path using a bash-native command (e.g. `cat >> file << 'EOF' ... EOF`) instead of the Edit/Write tools, then re-run the bash-side step. NOTE: a similar stale/truncated read has also been seen for freshly Edit-written files under the `ga40prj` mount (e.g. `messages/*.json` showing an "Unterminated string"); when in doubt, trust the Windows-side `Read` tool, not bash, for files just edited.
- **Zod v4 import.** The package is `zod ^4.x`. Always use `import { z } from "zod/v4"` — the default `"zod"` entry point re-exports v3 shims for compatibility and behaves differently. Also: `z.string().uuid()` is **strict** in v4 (validates version/variant nibbles) — test fixtures need real-shaped v4 UUIDs (`...-4xxx-8xxx-...`); real ids are `gen_random_uuid` (v4) so production is fine.
- **`AdvancedMarker` requires `mapId`.** Using `<AdvancedMarker>` without a `mapId` on the parent `<Map>` triggers the "This page can't load Google Maps correctly" error overlay on every render. Always pass `mapId={process.env.NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID ?? "DEMO_MAP_ID"}` to any `<Map>` that hosts `AdvancedMarker` children. `"DEMO_MAP_ID"` is Google's official dev placeholder and requires no Cloud Console setup.
- **Google Maps height chain.** `<Map style={{ height: "100%" }}>` only resolves when every ancestor has a concrete pixel height. `flex-1` alone (flex-algorithm height) does not satisfy this — wrap the map container in `<div className="relative flex-1 min-h-0"><div className="absolute inset-0">...</div></div>` to give it a concrete bounding box.
- **`@vis.gl/react-google-maps` event types differ by component.** `Map` component events give `MapMouseEvent` (library type) where `latLng` is a plain literal accessed as `event.detail.latLng?.lat` (property). `AdvancedMarker` drag events give `google.maps.MapMouseEvent` where `latLng` is a `LatLng` object accessed as `e.latLng?.lat()` (method call). Mixing these up is a silent runtime bug.
- **A disabled `<fieldset disabled>` disables EVERY descendant control** (inputs AND buttons), and a descendant cannot be re-enabled per-control. If a read-only form needs some still-clickable control inside it (e.g. version ◀/▶ nav, a toggle), render that control OUTSIDE the disabled fieldset — scope the fieldset to wrap only the truly-read-only inputs. (Hit in Slice #18.02: the version nav arrows died on read-only historical versions until the fieldset was narrowed to the cadastral + address sections.)
- **RHF `form.reset()` clears `isDirty`.** Any feature that programmatically resets the form (e.g. version navigation) must not rely on `form.formState.isDirty` for its "has unsaved changes" signal — compare current values to an explicit baseline held in state instead.
- **Auth: an expired Supabase session makes a save silently *look* successful.** The middleware redirects the mutating request to `/sign-in`; `fetch` follows the redirect and returns a 200 (the sign-in HTML), so the client thinks the save worked and navigates away — but nothing persisted, and a 401 shows on a token request in the console. Guard mutations with `if (res.redirected) throw ...` so the user gets a clear "sign in again" message. (Property, Natural-Person, Judicial-Person, and Document `doSave` paths all have this guard via a `saveErrorSession` key; add it to any new mutating form.)
- **4-column grid: skip the 3-column step.** For `columns={4}` in the `Section` helper, use `"grid grid-cols-2 gap-4 md:grid-cols-4"` — do not add a `md:grid-cols-3` intermediate. At common "half-width browser" sizes (768–1023 px) the 3-column class strands the layout on 3 columns instead of 4.
- **Drizzle `sql` template: a column object interpolated into a *correlated subquery* renders UNQUALIFIED.** Inside a `sql`` ` fragment used as a correlated subquery, `${someTable.id}` emits a bare `"id"` (not `"table"."id"`). If the subquery's own FROM exposes an `id` (e.g. it joins `group_member` + `property`), Postgres either rejects it as `column reference "id" is ambiguous` (SQLSTATE 42702) or — worse — silently binds it to the wrong table. Fix: reference the outer column as a **literal qualified name** in the template (e.g. `WHERE gm.group_id = groups.id`, not `= ${groups.id}`). Hit in Slice #18.07 (`listGroups` member-count + `getGroupDetail` candidate `otherGroupCount`/`NOT EXISTS`).
- **Migration file workflow — Claude follows these steps every time:**
  1. **Claude saves** the migration file to `src/db/<filename>.sql` (never just pastes SQL into chat).
  2. **Claude tells Adrian** to run `scripts\Apply-Migration.ps1` — that script compares `schema_migrations` against the files in `src/db/` and applies anything pending. Claude never provides manual `docker cp` + `psql -f` commands for a normal migration; just say "run `scripts\Apply-Migration.ps1`".
  3. **Claude states the filename** (e.g. `src/db/migration_035_seed_doc_types.sql`) so Adrian knows exactly which file to open.
  4. **Adrian pastes** that file's contents into the Supabase SQL Editor to apply it to the cloud database.

  **Exception — manual apply only when the script can't help:** if `schema_migrations` already has a false entry for a migration that was never actually run (the migration_056 backfill bug, hit in Slice #19.28), the script will skip it. In that case apply the specific file(s) directly:
  ```powershell
  docker cp src/db/migration_NNN_name.sql ga40prj-postgres:/tmp/mNNN.sql
  docker exec ga40prj-postgres psql -U postgres -d ga40db -f /tmp/mNNN.sql
  ```

- **`schema_migrations` can lie — "up to date" does not mean the tables exist.** `migration_056_schema_migrations.sql` created the tracking table and backfilled filenames 008–055 **by assertion** (a hardcoded `INSERT` list), not by inspecting the database. Any migration in that range that was never actually applied is nonetheless recorded as applied, so `Apply-Migration.ps1` finds nothing pending and reports success while the table is absent. The failure is silent until an API route 500s with `relation "..." does not exist`. Hit in Slice #21.09.help.error (`help_content` / `help_hint`, missing since Slice #16.UX.02). **Always run `scripts\Verify-Schema.ps1` after `Apply-Migration.ps1`** — it compares the `pgTable` declarations in `src/db/schema/index.ts` against `information_schema.tables` and is the only check that catches this. When it reports a missing table, apply that migration directly with `docker cp` + `psql -f` (the runner will keep skipping it) and then correct the `schema_migrations` row.
- **`supabase_schema_full.sql` is GENERATED — never hand-edit it.** It was hand-maintained until Slice #21.09.help.error, by which point it had drifted to 37 of 49 tables with 21 more missing columns, so every from-scratch cloud rebuild was already broken. Regenerate with `scripts\Export-SupabaseSchema.ps1` (dumps the live local DB) after any migration, and commit the result. For repairing an **existing** database additively — including production, which is safe because every statement is `IF NOT EXISTS` — use `src/db/supabase_repair_missing_tables.sql` instead; the full file assumes an empty schema.
- **When generating DDL for a table from its migration history, use the FINAL shape, not the creating migration.** Several tables are reshaped by later migrations: `migration_051` swapped `group_member`/`stamp_member`'s nullable `(person_id, property_id, document_id)` triple for a single `principal_object_id`; `entity_metadata` gained columns in 046 and 050, dropped `provenance_history` in 047, and had its provenance CHECK value set replaced in 067; `groups`/`stamps` gained `deleted_at` in 057. Lifting the `CREATE TABLE` out of the original migration reproduces a shape that no longer matches `schema/index.ts`.
- **DB migration reminders — display these at the start of any migration step:**
  - *Local Docker:* **Do NOT use `npm run db:migrate`** — it exits silently without applying the file (confirmed repeatedly). Use `scripts\Apply-Migration.ps1` instead (see above).
  - *Supabase:* Paste the migration SQL directly into the Supabase SQL Editor. If using `db:migrate`, first set `DIRECT_URL` to the direct connection string (port 5432, `?sslmode=require`): `DIRECT_URL=postgresql://postgres.[ref]:[password]@db.[ref].supabase.co:5432/postgres?sslmode=require`. Remove it again afterwards.
- **Coordinate axis order in Romanian cadastral text files**: The file columns are labeled `X [m]` (= Northing, ~300 000–850 000) and `Y [m]` (= Easting, ~200 000–800 000). This is the local Romanian geodetic convention where X points North — **opposite** to GDAL/PostGIS standard (X = Easting, Y = Northing). When calling `stereo70ToWgs84(north, east)`: pass the X column as `north` and the Y column as `east`. Valid Stereo70 range for the project area (Bragadiru, Ilfov): Northing ~320 000–325 000, Easting ~575 000–585 000.
- **`pg_dump` schema dump includes PostGIS `topology` schema — causes init conflict.** A schema-only `pg_dump` captures `CREATE SCHEMA topology;`. When this is used as a `docker-entrypoint-initdb.d` init script alongside `01-extensions.sql` (which creates `postgis_topology` and thus the `topology` schema first), psql hits `ERROR: schema "topology" already exists` and aborts with `ON_ERROR_STOP=on`. Fix: change `CREATE SCHEMA topology;` → `CREATE SCHEMA IF NOT EXISTS topology;` in the dump before shipping it.
- **`pg_dump` on Windows → UTF-16LE corruption.** PowerShell's `>` redirection saves files as UTF-16LE with BOM (`FF FE`). PostgreSQL's `psql` expects UTF-8; the null bytes corrupt SQL parsing and diacritics. **Never use `docker exec ... pg_dump > file.sql`.** Always let pg_dump write to the container filesystem, then copy out:
    ```powershell
    docker exec ga40prj-postgres pg_dump -U postgres ga40db -f /tmp/dump.sql
    docker cp ga40prj-postgres:/tmp/dump.sql ./dump.sql
    ```
    If you already have a suspect file, detect/fix encoding with `file`, `iconv -f UTF-16LE -t UTF-8`, strip CRLF (`sed -i 's/\r//'`) and BOM (`sed -i '1s/^\xEF\xBB\xBF//'`).
- **Non-ASCII characters in `.ps1` files break under Windows PowerShell 5.1.** It parses a no-BOM `.ps1` using the system codepage (the file-writing tools save UTF-8 without a BOM), so decorative non-ASCII (box-drawing `─`/`│`, smart quotes, em-dashes) becomes mojibake and can break tokenization (`ParserError: UnexpectedToken`). **Stick to plain ASCII in every `.ps1` file** — use `----`/`====` for dividers.
- **Relative paths break raw `[System.IO.File]` calls in `.ps1` scripts.** PowerShell cmdlets and external processes (`docker cp`, etc.) resolve relative paths against `$PWD`. Raw .NET static calls (`[System.IO.File]::ReadAllText`/`WriteAllText`) resolve them against the process-wide `[Environment]::CurrentDirectory`, which can silently diverge from `$PWD` (seen drifted to `C:\Windows\`). **Always resolve to an absolute path first** (`$repoRoot = $PSScriptRoot`; `[System.IO.Path]::GetFullPath((Join-Path $repoRoot "..\rel"))`) before any raw `[System.IO.File]` call.
- **Ciprian UAT schema + reference-data delivery (UC-C6, `GA40.Operations.Guide.05.docx`).** Every DB schema change is delivered to Ciprian as a single fixed-name file, `ciprian-schema-update.sql`, at the root of `C:\dev\ga40prj.Ciprian\`. It is **fully regenerated, never hand-edited**, every time `build-ciprian-image.ps1` runs (Step 5), assembled live from `ga40prj-postgres`: (1) `DROP SCHEMA public CASCADE; CREATE SCHEMA public;`; (2) Ciprian's `01-extensions.sql`; (3) `pg_dump --schema-only`; (4) `pg_dump --data-only -t 'lookup_*'`. Applying it **wipes Ciprian's entire UAT database** and rebuilds it to match dev (acceptable while Ciprian's data is disposable). **`build-ciprian-image.ps1` runs a pre-flight check (Step 0.5) before any dump: it queries `schema_migrations` in `ga40prj-postgres` and aborts if any `migration_*.sql` file in `src/db/` is unapplied — generating the schema from an incomplete DB would silently corrupt Ciprian's instance. Always run `scripts\Apply-Migration.ps1` first.** **Adrian never runs commands against `ciprian-ga40prj-postgres` himself** — that container is on Ciprian's PC; delivery always goes through the Ops Guide: schema/refdata change → **UC-C6** (send `ga40prj-app.tar` + `ciprian-schema-update.sql`); code-only → **UC-C5** (app tar only); full reset with no new version → **UC-C8**. `src/db/sync-reference-data.sql` / `npm run export:reference-data` are no longer part of the Ciprian flow. **Upcoming transition — data-preservation mode:** once Ciprian starts keeping real data (imminent), the full-wipe delivery will be replaced by a delta approach: identify which `migration_*.sql` files Ciprian has NOT yet applied (by querying his `schema_migrations` table), send only those, and apply them in order without touching his data. The pre-flight check already provides the foundation. A future slice will implement `ciprian-send-migrations.ps1` (or equivalent). Until then, the full-wipe flow remains in place.
- **Never put `--ignore-scripts` on the Dockerfile's `npm ci`, and never downgrade it to `npm install`.** `public/pdf.worker.min.js` is **gitignored** and exists only because the `postinstall` script (`scripts/copy-pdfjs-worker.mjs`) copies it out of `node_modules`. `--ignore-scripts` skips postinstall, so the file is never generated in the image; the builder's `COPY . .` then finds nothing to copy, because `.dockerignore` does not exclude `public/` but the file simply isn't there on a clean checkout. The result is a runtime 404 on `/pdf.worker.min.js` and broken PDF rasterisation in Admin → Import — **and only on a clean build**, so on a dev machine where postinstall has run at least once it looks like it works and the failure appears intermittent. `npm install` is separately wrong because it can rewrite the lockfile, so the image stops matching what CI built. Vercel is unaffected (it runs its own install with scripts enabled); this bites the Docker/Ciprian path only. Hit while landing Slice #19.28.e2e, where it had been applied as a workaround. Note the workaround was also unnecessary: no playwright package declares an install script (`hasInstallScript=false` in `package-lock.json`), so `npm ci` never tries to download browsers into the image.
- **`.dockerignore` does NOT inherit `.gitignore`.** They are independent files with independent rules. `e2e/.auth/session.json` holds a live Supabase session cookie and is gitignored — but until Slice #19.28.e2e added an explicit `e2e` entry to `.dockerignore`, it was still shipped into the Docker build context and baked into the builder layer. Whenever a new gitignored path holds a secret, add it to BOTH files.
- **Docker Compose `--env-file` does NOT inject every `.env` variable into a container.** It only makes values available for `${VAR}` substitution *inside the compose YAML*. A variable must be explicitly listed in the service's own `environment:` block (as `KEY: ${KEY}` or a literal) to be readable as `process.env.X` in the container. **When adding a new env var the app reads, also add it to the `environment:` block of every `docker-compose.yml` that runs the app** — local dev's and Ciprian's are separate files.
- **Never call `supabase.auth.getUser()` directly — use `getCurrentUser()` from `src/lib/auth/current-user.ts`.** A direct call has no idea about `UAT_NO_AUTH`, so it returns no user on Ciprian's box and any route that 401s there surfaces as "session expired" on a build with no login link (Slice #21.11.uat.auth). Same applies to reading `process.env.UAT_NO_AUTH` inside `src/` — import `isUatNoAuth()` instead. `middleware.ts` is the one legitimate exception, since it must decide whether to run the session refresh before any route code executes. `src/__tests__/auth-single-source.test.ts` enforces both rules. **The general lesson: when a bypass rule gets copy-pasted into a third place, stop and centralise it — the fourth site is the one that will be missed.**
- **Ciprian's UAT container runs with `NODE_ENV=production` but no real Supabase project.** Code that branches purely on `NODE_ENV === "production"` to use a cloud backend will fail there. Document-page storage has an override: `LOCAL_FILE_STORAGE=true` forces the local-filesystem path even in production. If a future feature adds a "real backend required" path, check whether it needs a similar override rather than trusting `NODE_ENV` alone.
- **A key in `KNOWN_TYPE_KEYS` with no seeded `lookup_document_type` row is a silent trap.** `src/lib/import/classify-prompts.ts` offers that array to Haiku as the valid classification vocabulary, and `scan-folder`/`ai-interpret` whitelist the model's answer against it — but `ensureDocType` then resolves the key through `typeMap`, which is built from the DB. An unseeded key passes the whitelist, misses `typeMap`, and falls through to auto-creating a type from the free-text label, so the document lands under a *generated* key. Anything matching on a literal key (e.g. `getPersonIdCardLink`, which looks for `CARTE_IDENTITATE`) then never finds it, and nothing errors. Found in Slice #23.01.Import, where six entries had drifted — one unseeded (`CARTE_IDENTITATE_ALT`) and five belonging to a different table entirely (`lookup_property_type`). **When adding a key to that array, add the migration in the same commit**, and cross-check with: `SELECT key FROM lookup_document_type ORDER BY key;` against the array. ⚠️ **Slice #23.08.Import removed the last surviving reference to `CARTE_IDENTITATE_ALT`** (it lingered in `ID_CARD_TYPE_KEYS` as a "defensive match" for two more slices after being struck from `KNOWN_TYPE_KEYS`). The lesson worth carrying: **an unseeded key cannot come back through the auto-creation path either** — `ensureDocType` generates a key from the free-text label, so it produces a *different* key, never the phantom one. A defensive match against a key nothing can emit is not defence, it is a lookalike sitting in a list of real things. Note also that `migration_021`'s three genuine alternate wordings (`AUTORIZATIE_ALT`, `CERTIFICAT_SARCINI_ALT`, `EXTRAS_CARTE_FUNCIARA_ALT`) all end in `_ALT` — the naming pattern is what made the phantom look legitimate, so match against the seed list, not against the suffix.
- **Import auto-creates document types from an AI label, with no confirmation.** Two independent code paths do it: `ensureDocType` in `bulk-import-dialog.tsx` (from the Haiku scan's `classifiedLabel`, deduped only within a single import run via `autoCreatedTypeCache`) and the extract branch of `ai-interpret/route.ts` (via `createValue`). Importing a folder of unfamiliar documents can therefore mint one `lookup_document_type` row per distinct label the model produces — a near-duplicate of an existing type ("Contract de arendă" vs "Contract de arendare") becomes a separate type, silently. Found while investigating Slice #21.10.Import; **left in place deliberately, to be fixed in its own slice** rather than folded into an investigation. Discover mode never does this. Check for drift with: `SELECT key, name, created_at FROM lookup_document_type ORDER BY created_at DESC;` — rows created long after the seeding migrations are the auto-created ones.
- **Check that a file under `src/app/admin/import/` is actually mounted before changing it.** For most of a year roughly 2,600 lines of that folder were unreachable: `/admin/import` renders **ImportWizard** (Slice #21.01.Import), not `ImportBrowser`, and nothing said so except a comment — so `import-browser.tsx` and everything only it mounted (`folder-scan-dialog.tsx`, `classify-dialog.tsx`, the three classify panels) still linted, still typechecked, and still got maintained. Slice #23.04.Import deleted all of it, so that specific trap is gone. The general one is not: the wrapper is still called `import-browser-dynamic.tsx` after a component that no longer exists, and `/admin/import` is still the only mounted entry point in the folder. Before fixing a bug there, confirm a user can actually reach the file — it is easy to spend a slice fixing code nobody runs.
- **The digit-prefix folder heuristic is gone from the wizard but ALIVE in two server-side places.** Slice #23.00.Import removed `parseFolderName` from the import wizard — a folder name starting with a digit is no longer read as `<tarla>-<parcela>-<rest>`, because it false-positived on ordinary names ("3 Calea Victoriei" → tarla "3"; "2024-Arhiva" → tarla "2024", parcela "Arhiva"). The function itself could **not** be deleted, because two consumers outside the wizard still call it and were deliberately left unchanged:
  1. **`src/app/api/documents/[id]/process/route.ts`** (steps 5–6) — the *Process* panel on a document detail page. This is a completely separate entry point from `/admin/import`, and it still has both original defects: it infers `tarlaSola`/`parcela` from a tag, and then calls **`findEntitiesByTag`** (its only caller anywhere) to associate every Document and Person in the system sharing that tag string — not just the ones from the current import. The `alreadyLinkedDocs`/`alreadyLinkedPersons` filter narrows this to entities not yet attached to *any* property, which reduces but does not remove the cross-linking risk.
  2. **`src/lib/metadata/queries.ts` → `addEntityTag`** — auto-generates `"47/2"`-style alias tags for any tag whose name passes the digit test. Now that tags link nothing this is cosmetic noise rather than a correctness problem, but it is the same guess. `tag-dialog.tsx` previews those aliases and was left calling `parseFolderName` on purpose: removing the preview while the server kept creating the aliases would make the dialog lie about what it is about to do.

  So: **do not reintroduce `parseFolderName` into the import wizard** *as a silent inference*, and treat a Process-panel bug report about wrong tarla/parcela or unexpected associations as this known gap, not a regression. Retiring it there is still its own slice — but the reason it *used* to be entangled is gone. **Slice #23.06.Import removed that coupling**: the 409 lock moved off `provenance = COORDINATE_FILE` and onto the `property_corner_source` table, so the check, the claim UPDATE, the catch-block reset and `process-panel.tsx`'s matching check no longer have to move together — they no longer exist. What remains in the Process route is only the `parseFolderName` call itself and `findEntitiesByTag`, which can now be retired on their own merits.

  ⚠️ **Slice #23.07.Import added the one sanctioned exception, and it is not a rollback of any of the above.** `PropertyStepDialog`'s create-new branch calls `parseFolderName` (via the pure `cadastralSuggestionFromFolderName` in `src/lib/import/coordinate-file.ts`) to PRE-FILL two visible, editable, Romanian-labelled tarla/parcela inputs that the user confirms before the Property is created. **The rule was never "this function is forbidden" — it is "the wizard must not decide a cadastral value alone and write it without showing anyone."** A suggestion the user sees and can correct before it is saved is a different mechanism from an inference, and it fails safely in a way the old heuristic could not: the wrong guess is on screen, next to a cursor. The suggestion is additionally stricter than the raw parser — it requires the full `<tarla>-<parcela>` shape, so the "3 Calea Victoriei" class of name yields two blank fields rather than a street address in a cadastral column. If you are here because you found `parseFolderName` imported under `src/app/admin/import/` or `src/lib/import/` and reached for the revert: check first whether the value is shown to the user before it is written. If it is, this is #23.07 working as designed.
- **A wizard-imported coordinate `.txt` arrives NOT "processed" — and until Slice #23.06.Import that produced a duplicate Property.** This entry previously claimed the opposite ("the wizard writes provenance `COORDINATE_FILE` … so opening that document afterwards shows the Process panel in its done/409 state … This is correct"). Every clause of that was wrong, and it was wrong in the most expensive direction: it described the bug as intended behaviour, so anyone hitting the bug would read this and stop investigating. Adrian found it the only way left — by noticing a flicker on the map where two identical polygons overlapped.
  **The real chain, confirmed by investigation:**
  1. `classifyFileSource()` (`src/lib/metadata/provenance-rules.ts`) maps a file by **extension only** and *deliberately never returns* `COORDINATE_FILE`. Its own comment says why: a `.txt` is indistinguishable from any other text file by name alone.
  2. `"txt"` is in `DOCUMENT_EXTENSIONS`, so `inferProvenanceForFiles` returns **`DOC_FILE`**, and that is what the wizard stamped on the imported coordinate document.
  3. `process-panel.tsx` decided done-vs-ready with `if (prov === "COORDINATE_FILE")`. `DOC_FILE` is not `COORDINATE_FILE`.
  4. The panel rendered **ready**. Pressing it built a second Property with identical coordinates, and that one populated Tarla/Parcela (the Process route calls `parseFolderName`; the wizard did not), so the two were not even equal. **That last asymmetry is closed by Slice #23.07.Import** — the wizard now SUGGESTS tarla/parcela into confirmable inputs, and applies the same `perToSlash` the Process route does, so the two paths agree on both the value and its encoding. The duplicate-Property half was already fixed by #23.06.Import.
  **Why the rule in step 1 is right and the design was still wrong:** the wizard is not guessing from a filename. `PropertyStepDialog` and `CoordinatePropertyDialog` both push the file through `POST /api/properties/parse-text` and count real corners. They had proof and threw it away, because the only channel available was a provenance code that another subsystem was entitled to infer differently.
  **The general lesson — this is the one worth carrying forward:** a display value doing duty as a lock will eventually be written by someone who only owes you the display contract. `provenance` was honest metadata to `classifyFileSource` and a mutex to the Process route, and neither was wrong on its own terms. Slice #23.06.Import gave the fact its own table (`property_corner_source`, `UNIQUE(document_id)`), so the lock is now a lock and provenance is now metadata.
- **OCR (Tesseract) — label text fuses with coordinate tokens.** Left-margin label text (e.g. `"SE A"`) fuses with the first numeric token of the **first data row** (`"SE A 1 321762.117"` → `"11321762.117"`), adding multiple leading digits. Handled via `trySplitMergedToken` (strips 1–3 leading digits) + rescue-2b in `parseTableFormat`. To debug a skipped corner 1, `console.log(rawText)` at the top of `parseOcrText` and inspect the terminal after a scan.
- **OCR (Tesseract) — common digit confusions.** `l`/`I` → `1`, `O` → `0`. `fixOcrDigits` in `scan-image/route.ts` corrects these before numeric parsing.
- **OCR (Tesseract) — do not pre-filter lines by keyword.** OCR sometimes merges the header row (with words like "Suprafata") into the first data row; a keyword filter would discard the real coordinates. Let coordinate-range checks reject non-corner values instead.
- **`\b` is ASCII-only — never use it to match Romanian text.** JavaScript's word-boundary assertion is defined over `[A-Za-z0-9_]`, so it does not count `ă â î ș ț` as word characters at all. This does not merely weaken a match, it inverts it: `/\bÎnch\b/i` can **never** match a string that starts with "Î", because at offset 0 the boundary test asks whether the first character is an ASCII word character, "Î" is not, and the match fails before any of the pattern is examined. The symptom is a silent non-expansion that looks like a missing dictionary entry rather than a regex bug — hit in Slice #23.03.Import, where the "Inch Intab" abbreviation had never once expanded for anyone who typed the folder name correctly. **Use Unicode-property lookarounds instead** — `(?<![\p{L}\p{N}])…(?![\p{L}\p{N}])` with the `u` flag — which ask the question actually meant ("is the neighbour a letter or digit, in any script?") and behave identically to `\b` for the ASCII inputs that already worked. The same applies to `\w`, `\W` and `\B`. Where the goal is comparison rather than matching, prefer the existing `foldRomanian` (`src/lib/import/id-card.ts`), whose NFD decomposition covers both the comma-below (U+0219/U+021B) and cedilla (U+015F/U+0163) encodings of ș/ț that appear in real data.
- **The Stereo 70 parser does not accept a comma DECIMAL separator, only a comma DELIMITER.** `src/lib/geo/stereo70-parse.ts` splits on `[\s,;|\t]+` first, so `"321762,117"` becomes the two tokens `"321762"` and `"117"` and the line is **rejected** (`parseLine` returns `null`) — the `.replace(",", ".")` inside is dead code, because by the time it runs a token can no longer contain a comma. Its header comment claimed otherwise until Slice #23.03.Import. The failure is safe (a rejected line stores no coordinate, so nothing wrong is ever written) but total: a comma-decimal cadastral file imports as **zero corners**, which surfaces as "the file has no coordinates" rather than as a parser problem. Left unfixed deliberately — `"1,321762.117,584000.250"` is a legitimate comma-delimited row that parses correctly today, so comma cannot mean both things at once; resolving it needs a per-file separator decision. Pinned by `src/__tests__/stereo70-parse.test.ts` so it can never regress from a rejection into a misread.

## Key paths

- `C:\dev\ga40prj` — this repo (read-write)
- `C:\dev.docs\ga40prj` — Adrian's reference docs (read-only): stack decisions, install logs, credentials, future mockups
- `C:\dev.docs\ga40prj\01.Slice.Inputs\` — Adrian's slice input docs (reference only; read only the folder relevant to the current slice)

## Reading order for a fresh session

1. This file (`CLAUDE.md`) — top-down.
2. `README.md` — local dev setup and common commands.
3. `package.json` — confirm exact versions before assuming any API.
4. Most recent `git log --oneline -20` — see what just shipped.
5. If a slice is in progress, the relevant folder under `C:\dev.docs\ga40prj\01.Slice.Inputs\` and any open work-in-progress branches.
